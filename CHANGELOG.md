# Changelog

## 2026-05-13

- [feat]: **Migrate button on the edit modal is now action-first — (s) Split now / (m) Move / (c) Copy.** Requested: "when i click the 'migrate' button on the edit task UI, it currently shows (1) tomorrow (2) fri, etc. I want it to show (s) split now, (m) move, (c) copy. Split now does the duplicate + migrate unfinished subtasks + check parent task. (m) then opens a second dialogue — when? (t)oday to(m)orrow (d) date (picks date). Then it asks me 'split' 'move whole'. This should not ask me to copy." Done: `buildMoveChoices` (`src/view.ts`) now returns three action entries (Split now / Move / Copy) instead of the prior day buttons; the inline calendar pick (`moveCalendarPick` plumbing + `dp-edit-move-calpopover` popover) is removed from the edit modal. (s) Split now fires `splitTaskToDate(file, task, tomorrow)` directly with no further prompts (tomorrow = `selectedDate + 1`). (m) Move and (c) Copy both run a new `migrateWithWhen(file, task, kind)` orchestrator: stage 1 opens a new `WhenModal` (today / tomorrow / pick-date — the date option swaps in `renderPickerCalendar` inline); stage 2 is `MoveActionModal` (split / move whole, no copy) for Move, or the existing `SubtaskScopeModal` for Copy. Replaced the prior `MigrateAction` 3-option type / `MigrateActionModal` with a 2-option `MoveActionKind` / `MoveActionModal`. Dropped the now-unused calendar popover handling in the edit modal's stage-two key handler. Settings doc reference updated from "Move to tomorrow → Migrate incomplete" to "Migrate → Split now" (`src/settings.ts`).

- [feat]: **New "Quick add task" command — pops the standard new-task modal from anywhere, targeting today's daily note.** Requested: "create a 'quick add Task' command that pulls up the edit task modal". Done: new `quick-add-task` command in `src/main.ts` (callback → `quickAddTask()`). The method prefers reusing an existing TodayView: it first scans `VIEW_TYPE_TODAY` leaves, then falls back to any shell leaf and asks `ShellView.getMountedTodayView()` (new public getter on `src/shellView.ts` that exposes the shell's currently-mounted embedded view when its target is "today"). When neither exists it activates the Today view first and then invokes the modal on the freshly-created instance. The modal itself is the same `openNewTaskModal` flow used by "New unscheduled task" — reached via a new `public quickAddUnscheduledTaskToToday()` method on `TodayView` (`src/view.ts`) that ensures today's daily note via `ensureDailyNote` and then delegates to the existing private `createUnscheduledTask(file)`. Save writes the task line via `appendTaskAfterLast` to today's note regardless of which date the Today view is currently showing.

- [feat]: **New "Log Brain Dump" journal flow — single freeform textarea, no template, lines become bullets.** Requested: "can you create a new journal 'Brain Dump' that's just a brain dump?". Done: added a fourth journal flow `brainDump` with a new `"dump"` format alongside `"qa"` (Begin/Close Day) and `"bullet"` (Distractions). The flow is templateless by design — `runJournal` (`src/journal.ts`) detects `format === "dump"`, opens a single roomier `QuestionModal` (10-row textarea, no question prompt, placeholder "Dump it…") via the new `askDump` helper, splits the answer with `formatDumpBullets` so each non-empty line becomes its own `- bullet`, and appends them in one shot under the `## Brain Dump` section in today's note. Skips the day picker (always today). Wiring: new `brainDump: JournalFlow` field on `JournalSettings` with default section title "Brain Dump" (`src/settings.ts`), settings-tab entry with a `hideTemplate: true` flag so the template-file row is suppressed (only section title is editable), `loadSettings` merge for the new key (`src/main.ts`), and a new `journal-brain-dump` command labelled "Log Brain Dump". `QuestionModal` was minimally extended with public `rows` / `placeholder` overrides and now skips the `.dp-journal-prompt` div when `question === ""`.

- [fix]: **Upcoming-events row (birthdays / anniversaries) now hides together with habits.** Requested: "when 'h' or 'b' are tapped, the birthdays should disappear — (they'll disappear with the habits)". Done: added `.dp-upcoming` to the `is-habits-collapsed` hide list in `src/styles.src.css` so the upcoming chips collapse alongside `.dp-habits` / `.dp-workout` / `.dp-quote-row` when `h` toggles habits or `b` toggles both. The keypress handlers in `src/view.ts` (lines 328–339) already set the `habitsCollapsed` flag — only the CSS rule was missing.

- [fix]: **Today shell layout — sidebar was getting cut off and the embedded view's content was occupying only a fraction of the right pane.** Reported: "There are some bad bugs there. It appears there's a second pane below" with a screenshot showing the sidebar truncated mid-"This week" and TodayView's date header at the top of the right pane with empty space and stray nav-buttons at the bottom. Root cause: `.dp-shell-app` had `height: 100%` directly on `contentEl`, but in some Obsidian theme contexts contentEl's height isn't a defined containing block — the 100% resolved against `auto`, the shell collapsed to the height of its sidebar items, and the embedded view's content rendered into a too-short host. Fix: contentEl now gets a marker class `.dp-shell-root` that zeroes its padding and makes it `position: relative`; `.dp-shell-app` is a child of contentEl positioned `absolute; inset: 0` so it fills the pane regardless of theme. Also removed the redundant `overflow-y: auto` on `.dp-shell-host-content` — the embedded view's own `.today-root` scroller was being nested inside an outer scroller, breaking sticky positioning for the date navbar. Files: `src/shellView.ts` (`renderChrome` adds class to contentEl + creates a child div for the app), `src/styles.src.css`.

- [feat]: **Today shell now inline-embeds the views — one SaaS-style window with a persistent sidebar.** Requested: "I don't want a controller that controls other obsidian tabs. I want the tabs viewable inside the today shell — like a proper SaaS app." Done: removed the `createLeafBySplit` approach. `ShellView` (`src/shellView.ts`) now renders `dp-shell-app` = `dp-shell-sidebar` + `dp-shell-host`, where the host is a two-child div (a hidden `dp-shell-host-stub` at `children[0]` plus the real `dp-shell-host-content` at `children[1]`). On nav click `switchTo(target)` tears down the prior `mounted` view (`onClose` + `removeChild`, then clears the content child) and mounts the next one: it instantiates `TodayView` / `MultiDayView` / `HabitsStatsView` with `this.leaf` (the shell's leaf), then overrides each instance's `containerEl` to the host div and `contentEl` to the content child — both views' render code reads `containerEl.children[1]` as its render root, so the embedded views paint inline. `addChild(view)` triggers Obsidian's Component lifecycle so `registerEvent` / `registerInterval` callbacks the views set up clean up automatically on swap. `activateShellView` (`src/main.ts`) is back to a plain single-leaf open — no split, no paired content leaf, no `setContentLeaf` plumbing. Styles in `src/styles.src.css` updated to the new flex layout (`.dp-shell-app`, `.dp-shell-sidebar`, `.dp-shell-host*`). Known: TodayView's `p` hotkey calls `moveLeafToPopout` / `leaf.detach` on `this.leaf`, which is the shell's leaf when embedded — pressing `p` inside the shell pops out (or detaches) the entire shell rather than just the TodayView pane.

- [feat]: **Today shell now keeps the sidebar persistent — clicking a nav row swaps a paired content leaf instead of replacing the whole leaf.** Requested: "Improve it so that the sidebar sticks around while the different tabs are checked." Done: `activateShellView` (in `src/main.ts`) now creates the shell as a vertical split — the left leaf hosts `ShellView` (sidebar nav only), the right leaf is a paired content leaf that starts on `TodayView`. The shell holds a direct reference to its content leaf and calls `contentLeaf.setViewState({ type })` on click; the nav itself never re-loads. `ShellView` (`src/shellView.ts`) was rewritten as a single nav column — drops the welcome card, gains an `is-active` highlight on the currently-open target. If the user closes the content leaf manually, `ensureContentLeaf` recreates it via `createLeafBySplit` on the next nav click (a `parent`-existence probe distinguishes attached vs. detached leaves). New CSS (`.dp-shell-nav`, `.dp-shell-nav-item`, `.dp-shell-nav-item.is-active`, etc. in `src/styles.src.css`) — the old `.dp-shell-content` / `.dp-shell-welcome*` blocks are gone.

- [feat]: **New "Today shell" launcher view — one pane with a sidebar that swaps the leaf to Today / This week / Reporting.** Requested: "Let's create a sidebar plugin that when clicked opens a pane. The pane has a sidebar: Today / This week / Reporting. Reporting has the 3 tabs. So essentially it's a single container for all three views." Picked approach: "switch the active leaf when a tab is clicked." Done: new `src/shellView.ts` registers `VIEW_TYPE_SHELL = "today-shell"`. The view renders a 200px sidebar (Today / This week / Reporting, each with a Lucide icon) and a content area with a welcome card that lists the same three entries with descriptions. Clicking any nav row or welcome row calls `this.leaf.setViewState({ type: <target> })` to swap the leaf in place to `TodayView` / `MultiDayView` / `HabitsStatsView`. The Reporting target opens `HabitsStatsView` which already has its own internal 3-tab navigation, so it slots in naturally. Wiring in `src/main.ts`: `registerView`, new `layout-dashboard` ribbon icon ("Open Today shell"), new command `open-today-shell`, and an `activateShellView()` method that prefers an existing shell leaf else opens a new tab. Styles in `src/styles.src.css` (`.dp-shell*`). The existing three ribbon icons and commands stay untouched.

- [feat]: **Begin Day / Close Day open a day-picker first (Today / Yesterday / Tomorrow).** Requested: "When I do a journal entry for the day, let me pick 'today', 'tomorrow', 'yesterday' so it can apply to those days … just journals. Cmd+Enter does Today by default, otherwise they need to click to choose." Done: new `DayChoiceModal` (three side-by-side buttons that reuse `.dp-dup-mode-row` / `.dp-dup-mode-btn`; each button shows the day label + the absolute date underneath). Cmd/Ctrl+Enter is registered on `this.scope` and picks Today; no button is focused by default, so plain Enter does nothing — the user must commit a choice (click) or use the shortcut. Escape cancels the whole flow before any daily note is touched. The picker only fires for the QA flows (`beginDay` / `closeDay`); Distraction stays no-friction and always logs against today. Notice copy now reads "Begin Day logged for yesterday." when the user picked a non-today option. Files: `src/journal.ts` (new `DayChoice`/`DAY_OFFSETS`/`DAY_LABELS`, new `DayChoiceModal` + `pickDay`, `ensureNoteForOffset` replaces `ensureTodaysNote`), `src/styles.src.css` (`.dp-journal-day-hint`).

- [feat]: **Journal & Logs — three new command-palette flows that log into today's daily note.** Requested: "Let's create a journal. Add a section to the daily note called `## Begin Day` (configurable) and append to whatever text is in there. Second flow: `## Close Day`. Third: `## Distractions`. Three commands: Log Begin Day, Log Close Day, Log Distraction. Each flow reads questions from a template file (one question per line, like `_quotes`), asks them in sequence via a modal, and writes each answer back to the section. Settings live under a new tab 'Journal & Logs'. Distraction has a different output: just appends a `- bullet` to the section (goal: getting ideas out quickly)." Done: (1) New `src/journal.ts` with `runJournal(plugin, flowKey)` orchestrator, a shared `QuestionModal` (textarea + Save/Cancel, Enter submits, Shift+Enter newlines, Escape aborts the remaining questions but keeps prior answers), and a pure `insertIntoSection` string transform that finds the `## <title>` section or appends one at end-of-file, then inserts the new Q+A block / bullet at the end of the section (case-insensitive heading match, blank-line separator between successive blocks). Each step is written via `app.vault.process` after the modal resolves so partial completion is durable. (2) Begin/Close Day write `> question` + answer blocks; Distractions writes `- answer` bullets, dropping the question text from the output. Empty submits skip writing but advance to the next question. (3) Three commands wired into `src/main.ts`: `journal-begin-day`, `journal-close-day`, `journal-distraction`. (4) New `journal: { beginDay, closeDay, distractions }` block on `TodaySettings` — each entry is `{ sectionTitle, templatePath }`. Defaults: `Begin Day` / `Close Day` / `Distractions` titles with empty template paths. (5) New "Journal & Logs" settings tab (`book-open` icon) added at the end of the tab strip, with three sub-headings — each renders a section-title text input and a `FileSuggest`-backed template-path input. (6) Tiny modal styles in `src/styles.src.css` (`.dp-journal-prompt`, `.dp-journal-input`). Files: `src/journal.ts` (new), `src/main.ts` (loadSettings merge + command registration), `src/settings.ts` (types, defaults, tab, render methods), `src/styles.src.css`.

- [feat]: **`@`-autocomplete keeps the popover open through one space so two-word person names work.** Previously typing `@Bob` followed by a space closed the popover, making it impossible to filter or create a person named "Bob Dylan" via the `@` trigger. The query gate in both the inline editor suggester (`src/main.ts`, `onTrigger`) and the modal title-suggester (`src/view.ts`, the shared date/people rule's `acceptQuery`) only allowed whitespace when the query matched the `^[A-Za-z]+ \d{0,2}$` date keyword pattern (e.g. `@apr 5`). Relaxed both to allow any single whitespace span (`!/\s.*\s/.test(q)`) — single-space date queries still match, two-word names like `@Bob Dylan` now stay active, and a second space still closes the popover so other text on the line can't keep it open forever. The user asked: "when i type @ for a name, and I hit space, it removes the auto-complete. instead, let's keep it until the second word… so that I can create a new person with Bob Dylan."

- [feat]: **Unified per-task migrate flow with Split / Move / Copy.** Removed the standalone "Duplicate" button (and `y` hotkey) from the edit modal. The existing date-picker buttons (tomorrow / +2 / +3 / next week / calendar) now route through a two-stage picker: stage one picks Split / Move / Copy with a one-line description on each; stage two (Copy only) picks sub-task scope (all / unchecked / none). Semantics — Split: source parent → `[x]` + shared `#tid/<id>`, unchecked sub-tasks stay on source as `- [>]` (other metadata untouched), new day gets a fresh `[ ]` parent (same tid) + the unchecked sub-task lines verbatim. Move: source parent → `[>]` + shared `#tid/<id>`, all sub-tasks stripped from source, new day gets fresh `[ ]` parent (same tid) + every sub-task line verbatim. Copy: source untouched, new day gets a stripped-tid clone with the chosen sub-task scope. New `MigrateActionModal` (stage one) + `SubtaskScopeModal` (stage two) modals, new types `MigrateAction` + `SubtaskScope`, and three new methods on `TodayView` (`splitTaskToDate`, `moveTaskToDate`, `copyTaskToDate`) orchestrated by `migrateTaskToDate`. The old `duplicateTask`, `migrateIncompleteToDate`, `moveTaskWholeToDate`, `onDuplicate` / `hasSubtasks` options, and `DuplicateSubsModeModal` are gone. Files: `src/view.ts`, `src/styles.src.css` (added `.dp-dup-mode-btn-label` + `.dp-dup-mode-btn-sub` for the stage-one stacked labels; `text-align: left` on the buttons). The user asked: "right now we have two flows, duplicate and migrate… migrate should have 1) split, 2) move 3) copy… get rid of the duplicate button."

- [feat]: **Duplicate flow now lets you pick which sub-tasks carry over: all, only unchecked, or none.** Previously the duplicate button popped a native `window.confirm("Copy sub-tasks too?")` — a binary all-or-nothing choice. Replaced with a small in-app `DuplicateSubsModeModal` (three side-by-side buttons, escape/close = cancel) so recurring checklists can be re-launched with just the unfinished items. New tri-state `DuplicateSubsMode = "all" | "unchecked" | "none"` threaded through `TaskEditOpts.onDuplicate` and `duplicateTask` (filters `task.subtasks` by `s.checked` before resolving line numbers). Tasks with no sub-tasks skip the picker and pass `"none"` straight through. Notice copy reflects the picked mode ("with sub-tasks" / "with unchecked sub-tasks" / plain). Insertion point still anchors past the *last* sub-task line of the source so the new block lands cleanly beneath, even when copying a strict subset. Files: `src/view.ts` (`duplicateTask`, `runDuplicate`, new `DuplicateSubsModeModal` class), `src/styles.src.css` (`.dp-dup-mode-row` + `.dp-dup-mode-btn`). The user asked: "when i duplicate a task, let me choose to copy all subtasks, or just non-completed subtasks, or no subtasks."

- [feat]: **Picking a prior task from the title autocomplete now pulls in its project too.** `applyPriorPick` (`src/view.ts`) previously copied only `sugg.title` into the title input — by design, per the comment block — so the user had to retype the project for every recurring task. Added `projInput.value = sugg.project ?? "";` next to the existing title write; the trailing `updateSummary()` already covers the summary refresh, and `sugg.project` is the pre-composed `proj/sub` string the project input expects (matches what `attachProjectSuggest` populates on pick). Duration, description, tags, and creation date still reset to fresh defaults so each pick produces a new task — only title + project carry over. Comment block above the function updated to reflect the new behavior. The user asked: "when i'm typing a new task and it auto-completes from a prior task, have it pull in the project. right now it just pulls in the prior task title, but i also want the project pulle din."

## 2026-05-12

- [fix]: **Mobile timeline-block title is only clickable on the text, not the full bar.** The `is-mobile-condensed` variant (added in 1508fe8 to condense blocks to "start time + title" on mobile) set `.dp-block-text { flex: 1 1 auto }`, which stretched the title span to fill the entire remaining row width. Since the click handler that toggles done is bound to that span, the empty space past the text registered as a title tap instead of falling through to the block's edit handler. Changed flex-grow back to 0 (`flex: 0 1 auto`, matching the desktop default) so the span hugs its text; shrink stays at 1 so long titles still wrap inside the row. File: `src/styles.src.css` (`.dp-block.is-mobile-condensed .dp-block-text`). The user reported: "on mobile, the task title is clickable across the entire bar. I just want the text clickable. Perhaps a recent change on making it more compact affected it."

## 2026-05-11

- [feat]: **Task edit modal now has a collapsible "Created" field, and picking a prior task from autocomplete loads the title only.** Requested: "make a property on the edit modal for 'created' (#tc/YYYY-MM-DD). By default, hide this under a collapsible item in the edit modal. Every time a new task is created, this gets added. but it's hidden from view. Also, when i load an old task, it should only load in the old title. Not the duration, the creation date, or any other metadata. All of that should be created afresh." Done: (1) Added a `<details>`-based "Created" collapsible to the edit modal (`src/view.ts` `TaskEditModal.onOpen`) showing a `type="date"` input pre-filled with the parsed `#tcr/YYYY-MM-DD` value (or today's date in "new" mode). Summary header shows the current value next to the disclosure caret. Closed by default. New CSS in `src/styles.src.css` (`.dp-edit-created*`). (2) New `initialTaskCreated` on `TaskEditOpts` wired in from `openTaskEditor` via `parseTaskCreated` and from `openNewTaskModal` as `todayDateStr()`. (3) Extended the `onSave` callback signature with a `taskCreated` parameter (`undefined` = leave alone, `""` = clear, otherwise set). (4) New parser helpers `replaceTaskCreatedTag` and `removeTaskCreatedTag` in `src/parser.ts` for replace/remove semantics (existing `setTaskCreatedTag` was append-only). `applyTaskEdit` in `src/view.ts` uses them. (5) `cleanBody` now strips `#tcr/<date>` so the title input doesn't leak the tag. (6) `applyPriorPick` (autocomplete pick) now sets *only* the title — no description, project, duration, tags, or creation date. Subtask copying still respects the existing `copySubtasksOnAutocomplete` setting.

- [feat]: **Long timeline-block titles now wrap to a second line.** The default `.dp-block-text` was `white-space: nowrap` + `text-overflow: ellipsis`, so a block like `11a–11:30a · workday /service-ownership · Deployment #tc/2026-…` got truncated when both the project and title were long. Switched the default `.dp-block-row` to `flex-wrap: wrap` and the default `.dp-block-text` to `white-space: normal` + `overflow-wrap: anywhere` so the title flows onto a second line when it can't fit alongside the time/project. Preserved existing single-line ellipsis for `.is-compact` blocks (`<25m`/`≤20m` depending on view — no vertical room to wrap); narrow multi-column and mobile-condensed variants are unaffected (their own overrides win on specificity/order). File: `src/styles.src.css`. The user asked: "let's improve the task title visibility. I like how it wraps for two columns and how it's smaller on mobile. the projects are getting long, and the title isn't wrapping. so let's wrap the title."

- [fix]: **Today view no longer flickers while typing in unrelated notes.** Both the `vault.modify` and `metadataCache.changed` handlers in `src/view.ts` were calling `scheduleRender()` on any TFile, so every keystroke anywhere in the vault fired a 100ms-debounced `root.empty()` + full rebuild. Filtered both handlers to only re-render when the modified file's path matches the currently-selected day's daily note (new `currentDailyNotePath()` helper using `getDailyNoteOptions` + `formatDate` + `normalizePath`). Edits to other files no longer touch the view; navigate the calendar to refresh against a different day. The user asked: "why does the today plugin flicker as I'm typing on a random note?" then "filter the handlers to only re-render when the associated day is edited."

- [feat]: **New-person template.** Settings → People → "New-person template" lets you point at a vault markdown file (FileSuggest-backed text input) whose contents become the body of every new person note auto-created via the `@Name` autocomplete. Supports `{{name}}` (the sanitized basename) and `{{date}}` (today as `YYYY-MM-DD`) as placeholders; unknown `{{…}}` tokens are left for the user to fill in. Empty path falls back to the prior auto-stub (frontmatter seeded from `upcomingFields`). If the template path is set but the file is missing at create time, a Notice surfaces "Person template not found — using default stub" so it doesn't fail silently. Files: `src/settings.ts` (new `personTemplate` field + UI under People folder), `src/people.ts` (split `createPerson` into a `buildPersonContent` + `expandPersonTemplate` + `buildAutoStub` trio so template / stub paths share a single create-and-notify flow), `src/main.ts` (passes `settings.personTemplate` into `createPerson`). The user asked: "can i have a new person template?"

- [feat]: **Upcoming events now supports arbitrarily many configurable date fields.** Replaced the fixed `birthday`/`anniversary` pair with a list of `{field, icon}` rows in Settings → People → Upcoming events → Date fields. Defaults still seed birthday/cake and anniversary/gem; add `deathday`, `work-iversary`, `graduation`, etc. with their own Lucide icons. Each event in the dashboard header uses the icon from its matching field config; tooltip shows the field name. Settings migrate the prior four-key shape (`birthdayField`/`birthdayIcon`/`anniversaryField`/`anniversaryIcon`) into `upcomingFields[]` on first load, then strip the legacy keys from `data.json` so they don't persist. `createPerson` (auto-create from `@Name`) now seeds frontmatter for every configured field, not just the two. Files: `src/upcoming.ts` (new `UpcomingFieldConfig`, `DEFAULT_UPCOMING_FIELDS`; `UpcomingEvent` now carries `field` + `iconName` instead of `kind`; scanner iterates configured fields), `src/settings.ts` (new fields-list section with in-place add/remove; intro text), `src/main.ts` (`resolveUpcomingFields` migration + legacy-key strip; `createPerson` call uses `seedFields`), `src/view.ts` (renders `ev.iconName` directly; tooltip uses `ev.field`), `src/people.ts` (`createPerson` takes `seedFields: string[]` instead of two named fields). The user asked: "update the settings to look at any number of date columns. by default 'birthday' 'anniversary'. but let me add some more. this is configurable."

- [fix]: **"Add tag override" button now appends the row immediately.** Previously the handler followed the contextTags/projectColors pattern of pushing to the array, awaiting `saveSettings()`, then calling `this.display()` + `focusLastInput()`. For this list specifically the full re-render didn't surface the new row until the settings pane was closed and reopened — likely a timing/scroll interaction with the People tab's larger pre-button section. Refactored to in-place row append: the row's handlers close over the `entry` object identity (using `indexOf(entry)` only inside Remove, never raw numeric idx), so they survive splices of earlier rows. The Add button pushes the entry, calls a new `appendRow(entry)` helper, scrolls the row's name input into view, focuses it, then persists asynchronously — no full `display()` call needed. List `<div>` is created above the Setting and then `appendChild`ed after it to keep the visual order (Add button on top, rows below). File: `src/settings.ts` (`renderUpcomingSection`). The user reported: "when i click 'Add tag override' button, it doesn't show me a new tag in the moment. I need to close the settings and re-open."

- [feat]: **`@Name` autocomplete now offers to create a new person when there's no match.** Typing `@Bob` while no person file named "Bob" exists in the People folder shows a final suggestion `Create new person: Bob — in <peopleFolder>`. Selecting it creates `<peopleFolder>/Bob.md` with a minimal frontmatter stub seeded with the configured birthday/anniversary keys (empty values), then inserts a wiki link at the trigger position. Skipped when the query matches a date keyword exactly (so `@today` doesn't tempt a literal "today" person). Files: `src/people.ts` (new `createPerson`, `findPersonByName`, `sanitizePersonName`), `src/main.ts` (extended `SuggestItem` with `action: "create-person"` + `payload`, added entry to the date-branch of `getSuggestions`, async handling in `selectSuggestion` that captures `context` before `close()` so the editor range is still valid when the async create resolves). The user asked: "update autocomplete so that if i do @{Person Name} and the person name doesn't exist, it asks me 'do you want to create a new person'? and it'll create a person in the designated people folder."

- [chore]: **New "People" settings tab** — moved the People folder + Upcoming events configs out of the View tab into a dedicated tab (Lucide `users` icon). Tab order is now: Automations · Templating · Tasks · Day · View · Projects · People · Pomodoro · Habits. Added `people` to the `SettingsTab` union + `TAB_SPECS`, added a switch case calling new `renderPeopleSection`, and moved the two render blocks. View tab no longer mentions People. File: `src/settings.ts`. The user asked: "create a new UI section called 'People'. put in here the config for people folder."

- [feat]: **Upcoming birthdays + anniversaries surface in the Today view header.** Scans the People folder for `birthday` / `anniversary` frontmatter fields (configurable field names), parses flexible date shapes — `1990-05-15`, `05-15`, `May 15`, `May 15, 1990` — and renders a row beneath the quote like `🎂 Bob — May 15 (7d)` for each event whose next occurrence falls inside the lookahead window. Stays visible every day through the event date, then disappears. Lookahead defaults to 7 days; per-tag overrides (e.g. `p/family → 21 days`) let you flag close family/friends for 3-week alerts — the largest matching override wins. Icons (`cake` / `gem` by default) are Lucide-name strings and customizable. Tags are matched against both inline hashtags and frontmatter `tags:`. New file `src/upcoming.ts` (scanner + date parser + countdown), settings additions in `src/settings.ts` (new "Upcoming events" section under View tab), view wiring in `src/view.ts` (`renderUpcomingRow`), styles in `src/styles.src.css` (`.dp-upcoming`). Defaults seeded in `DEFAULT_SETTINGS`; `upcomingTagOverrides` is filtered on load in `src/main.ts` to drop corrupt entries. The user asked: "there's a birthday's column in my people folder. also anniversary. I'd like to see anything upcoming in 7 days. I'd also like to customize so that anything with a specific tag can get notifications earlier… use 'cake' lucide icon for birthday, and 'gem' for anniversary - have both of these be customizable."

## 2026-05-10

- [fix]: **"Add project color" / "Add context tag" buttons appeared broken** — clicking them did push a new empty row to `projectColors` / `contextTags` and save (visible in `data.json`), but the subsequent `this.display()` rebuilds the entire settings pane and resets the scrollable container to the top, so the newly-appended row scrolled off-screen at the bottom of the list. The user kept clicking, accumulating multiple empty entries, and concluded the buttons did nothing. Fix: tagged each list with `data-list="project-colors"` / `data-list="context-tags"` and added a `focusLastInput(listName)` helper that, after `display()`, finds the last `.dp-project-color-name` input in the named list, scrolls it into `{ block: "center" }`, and focuses it — so the new row lands visibly under the cursor with the input already active. Wrapped in `requestAnimationFrame` so the query runs after the rebuilt DOM is committed. File: `src/settings.ts` (new `focusLastInput` method, called from both "Add" `onClick` handlers; `data-list` attr added to both list divs). The user reported: "i can't add new project colors or task context colors. please fix those 'add project' and 'add context' buttons so they work."

- [fix]: **Project-color drag-and-drop reorder now actually moves rows.** First pass attached `dragover`/`drop` to each row, but when the cursor was over an `<input>` inside another row those events didn't propagate reliably and the splice never ran. Lifted both handlers up to the list element, computing the hover row and insertion side from `event.clientY` against each row's bounding rect (above-midpoint = drop above; below = drop below; past the last row = append). Drop indicator hides when the computed destination equals the source (no-op move). Also switched the handle from `setAttr("draggable", "true")` to the `handle.draggable = true` property setter (matches `multiDayView.ts`'s pattern) and added `pointer-events: none` to the inner Lucide SVG so the drag unambiguously originates from the handle div, not the SVG. Files: `src/settings.ts` (`renderProjectColorsSection` — list-level dragover/drop with `computeTarget` + `clearIndicators` helpers), `src/styles.src.css` (`.dp-project-color-drag .svg-icon { pointer-events: none }`). The user reported: "the DND interface is there on the projects tab, but they don't actually move."

- [feat]: **Project colors are now drag-and-drop reorderable in settings.** Each row in Settings → Projects → Project colors gains a left-side grip handle (`grip-vertical` Lucide icon) — drag it onto another row to reorder. Drop indicator (a 2px accent-colored line) appears above or below the target row depending on which half of the row the cursor is over. Source row dims to 0.4 opacity while dragging. On drop, splices the `projectColors` array, saves settings, and re-renders. Files: `src/settings.ts` (`renderProjectColorsSection` — handle + dragstart/dragover/drop/dragend wiring with a closure-scoped `dragSrcIdx`), `src/styles.src.css` (`.dp-project-color-drag` handle + `.is-dragging` / `.is-drop-above` / `.is-drop-below` row states). The user asked: "in the settings, let me re-order the projects. (drag n drop)"

- [feat]: **Weekday templates now slot into the base via a `<@dow_template>` placeholder** instead of appending to the end. Put `<@dow_template>` wherever the day-of-week content should land in the base daily-note template (e.g. `_day.md`), and the matching weekday file (e.g. `monday.md`) is substituted there at note-creation time. Bases without the placeholder no longer get weekday content appended — users opt in by editing the base. Empty weekday entries collapse the placeholder to "". Files: `src/dailyNote.ts` (`readCombinedTemplate` rewritten as a placeholder substitution; interface doc updated), `src/settings.ts` (interface doc + the user-facing "Per-weekday templates" copy now explains the placeholder model). The user asked: "instead of just appending the day's tasks, let's create `<@dow_template>` and insert the day's template right there."

## 2026-05-09

- [fix]: **Multi-day gutter click now works standalone** — no longer requires a TodayView leaf to already be open. Replaced the "show Notice if no TodayView open" branch with a new `ensureTodayView()` helper: returns the existing TodayView if one exists, otherwise creates a leaf in the right sidebar via `getRightLeaf(false)` + `setViewState({ type: VIEW_TYPE_TODAY, active: false })` so focus stays on the multi-day view (no flash, no steal). The background TodayView hosts the TaskEditModal and inherits every existing save / Pomodoro / open-line post-action — no logic duplication. File: `src/multiDayView.ts`. The user asked: "it should work standalone always."

- [feat]: **Multi-day gutter is now clickable** — clicking an hour label in any day column's left gutter opens the same "new task at HH:MM" modal the daily-view gutter opens, anchored to that day. Mirrors the daily view: hour labels gain `is-clickable`, hover-tints to the accent color, and stop-propagation so the click doesn't bubble to the lanes drag handler. Click flow: ensures the day's daily note exists (calling `ensureDailyNote` with the standard `buildFallback(settings)`) so brand-new days get their template applied, then delegates to `TodayView.createTaskAtTimeForDay(file, date, startMin)` — a new public method (mirrors `openTaskEditorForDay`) that sets `selectedDate` + `calendarMonth` so the modal's date pickers anchor to the clicked day, then forwards to the existing private `createTaskAtTime`. If no TodayView leaf is open the modal can't render, so we surface a Notice explaining to open it. The closing endHour label stays inert (clicking it would create a task past the visible end of day). Files: `src/multiDayView.ts` (new `createTaskAt(day, startMin)` helper, gutter loop adds `is-clickable` + click handler), `src/view.ts` (new `public createTaskAtTimeForDay`), `src/styles.src.css` (`.dp-md-timeline-label.is-clickable` cursor + hover color). The user asked: "on the multi-day view, i should be able to click the gutter and add a new task the same way i can do it on the day view."

- [chore]: Reporting → Projects pivots now display durations as **decimal hours** (e.g. `3.5`) instead of `3h 30m` for at-a-glance readability across the row/column totals + footer. Added `formatHoursDecimal(mins)` in `src/parser.ts` (two-decimal max, trailing zeros trimmed: 210→`3.5`, 30→`0.5`, 120→`2`, 75→`1.25`). Swapped every `formatTotal()` call inside `renderProjectsPivot` (cells, tooltips, row totals, footer) to the new helper; the schedule labels and main-view stats panel still use `formatTotal`. File: `src/habitsView.ts`, `src/parser.ts`. The user asked: "on the reporting, don't have 3h 30m. have 3.5 so it's easy to read."

- [feat]: Reporting → Projects pivots now include an **Uncategorized** row covering completed tasks with no `[[project]]` link, instead of silently dropping them. Sentinel name `Uncategorized` is sorted to the bottom regardless of total (it's a catch-all bucket, not a tracked project) and rendered with a fixed neutral-gray swatch so it doesn't burn an auto-assigned palette slot. Updated the empty-state copy from "no completed tasks with a project" → "no completed tasks" to match. File: `src/habitsView.ts` (`renderProjectsPivot`). The user asked: "on the reporting on Projects, show 'uncategorized' (tasks without a project)."

- [fix]: **Timeline blocks were silently doubling their width** — every block painted across 2 columns at minimum, so two overlapping tasks both rendered at 100% width and stacked on top of each other instead of splitting 50/50. Cause: an earlier fix introduced `span = leftExt + 1 + rightExt` for two-direction extension, but `rightExtensionCols` still started its counter at 1 (the old "includes own column" semantics) while `leftExtensionCols` correctly started at 0. So `span` was inflated by exactly one for every block. Fix: changed `let ext = 1` → `let ext = 0` in `rightExtensionCols` so both helpers now return EXTRA columns only, matching the call site's formula. With the fix, two simultaneous tasks split 50/50 (span=1+0+0=1 each), three split 33/33/33, and the previous transitive-grouping cases (a non-overlapping block in a later column reclaiming the left/right empty slot) still work because the extension counters correctly walk past empty columns. Updated the doc comment to spell out the "extras only" semantics so the next person doesn't re-introduce this. File: `src/scheduler.ts`. The user reported (with screenshots): "they shouldn't be overlapping. they should split 50-50 into columns or 33-33-33."

- [fix]: **Removed the 2-lane cap on multi-day timelines** so overlapping tasks always split the day column N-ways instead of stacking visually after lane 2. Previously, three concurrent tasks would render as two side-by-side lanes with the third painted on top of one of them; now they render as three side-by-side lanes (or four, etc.). Trade-off the user explicitly chose: a 4-way overlap in 7-day mode shrinks each block to ~25% of a narrow column, but no two blocks ever cover each other. Dropped the `TIMELINE_LANE_CAP` constant and stopped passing `maxColumns` into `layoutTimeline()` — the daily view already calls it without a cap, so multi-day now matches. File: `src/multiDayView.ts`. The user asked: "I still want the tasks to split the vertical space when they're at the same time period. I don't want the tasks overlapping visually if they're happening at the same time."

- [feat]: **Projects tab now stacks three pivots — Day, Week, Month** — instead of just the weekly pivot. Day is fixed at 7 columns ("last 7 days"); Week + Month each follow `habitsStatsWindow`. Each section has the same Habits-tab-style title row ("Day · May 3 – May 9, 2026") and the same project × bucket pivot table with per-row totals on the right and a column-totals footer. Refactored the original `renderProjectsByWeek` body into a generic `renderProjectsPivot(parent, name, period, buckets, fallback)` so the day/week/month variants share aggregation + table rendering; the public method now just builds the three bucket arrays and calls the helper three times. Per-period column header format chosen by a new `formatPivotColLabel(bucket, period)` helper — Day = weekday short ("Sat"), Week = M/D ("5/4"), Month = month abbreviation ("May"); full date stays in the tooltip. The HabitsScanner cache makes the extra reads cheap (months section populates the cache that day + week then reuse). File: `src/habitsView.ts`. The user asked: "you built a week view. Now add a daily row showing last 7 days, then the weekly, then a monthly below it."

- [fix]: Multi-day blocks rendered at half-width even when they had no actual time conflict, when transitive grouping pushed them into a later column. Example: group `[A 7:30–13:00, B 11:30–13:00, C 8:30–9:00]` — A's long span makes col0 unavailable to B and C at packing time, so they both land in col1; B legitimately overlaps A and stays at 50%, but C has zero overlap with anyone and was still squeezed into the right half. Cause: the previous fix added rightward extension only — a block in col1 could grow further right but never reclaim the empty col0 space its time slot didn't actually consume. Fix: added a symmetric `leftExtensionCols` and changed the layout loop to compute `span = leftExt + 1 + rightExt`, with `leftPct = (idx - leftExt) * slotPct` and `widthPct = span * slotPct`. Two blocks can each claim the same neighbor column slot during disjoint time windows — they never visually overlap because the column-collision check gates extension on real time overlap, not column ownership. Refactored the duplicated `some(...)` predicate into a shared `columnCollides(col, tStart, tEnd)` helper used by both extension functions. File: `src/scheduler.ts`. The user reported: "why are the calendar items not taking up the whole space when there's no conflicting item?"

- [feat]: Renamed the **Habit stats** view to **Reporting** and added a third tab — **Projects** — alongside Habits and Workouts. The Projects tab is a pivot table of completed-task minutes (only `- [x]` lines with a `#p/<project>` tag count): rows are projects sorted by row-total desc, columns are weeks across the configured `habitsStatsWindow`, with row totals on the right and a column-totals footer row. Each row label gets the project's color swatch (resolved via the existing `resolveProjectColors` so the palette matches the daily view). Renamed user-facing strings only (the `VIEW_TYPE_HABITS_STATS` constant and command id `open-habits-stats` are unchanged so saved workspace state and any user-bound hotkey survive). Files: `src/habitsView.ts` (new `renderProjectsByWeek` + tab plumbing, heading text, `getDisplayText`), `src/main.ts` (command name + popout-command name), `src/styles.src.css` (swatch alignment in pivot row labels + `tfoot` divider). The user asked: "let's rename 'habit stats' to 'reporting'. Add a third tab to show project breakdown by week." Clarified: pivot table with row + column totals, only completed tasks count.

- [feat]: Multi-day timeline blocks now render with the same `dp-block` component the daily view uses, gain top + bottom resize handles for adjusting duration without re-dragging, and route clicks to the daily-view edit modal. Added a 32px left gutter inside `.dp-md-timeline` so hour labels live in their own column instead of getting overlapped by full-width blocks (this was the visible "spacing" bug — single-task lanes were 100% wide and ate the labels). Block markup: switched from a `<button class="dp-md-timeline-block">` to a `<div class="dp-block dp-block-md">` carrying the standard `.dp-block-row > .dp-block-meta + .dp-block-time + .dp-block-text` structure, plus the existing `.is-narrow` / `.is-compact` / `.is-done` / `.has-project-color` modifiers — so the daily view's color / padding / hover styling applies for free. New `.dp-block-md` overrides shrink the type, force single-line title truncation, and hide the time chip on `.is-narrow` lanes (no room when two blocks share a column). Resize handles reuse `.dp-resize-handle` and `.dp-resize-handle-top` from the daily-view CSS; bottom-handle drag adjusts duration only (writes `#d/`), top-handle drag anchors the end and shifts both `#t/` + `#d/`. Click delegation: the multi-day view looks up an existing `TodayView` leaf and calls a new `public openTaskEditorForDay(file, task, date)` on it, so the modal's move-picker / calendar anchor to the clicked task's day rather than whatever date the daily view happened to be on. No TodayView leaf? falls back to opening the file at the task's line. Inbox-row clicks route through the same modal (anchored to today, since inbox tasks aren't tied to a day). Files: `src/multiDayView.ts` (new `renderBlock`, `beginResize`, `beginResizeTop`, `applyDurationChange`, `applyStartAndDurationChange`, `openEditor`; gutter+lanes split inside `renderTimeline`; inbox click rerouted), `src/view.ts` (new `public openTaskEditorForDay`), `src/styles.src.css` (gutter/lanes layout, `.dp-block.dp-block-md` overrides, removed the obsolete `.dp-md-timeline-block` rules + the unlayered button override at file end). The user asked: "in multi2, let's make sure the events are spaced properly. some are getting really wide. And i should also be able to expand them up/down instead of just dragging up/down. i should be able to increase the duration by dragging the components up/down. If possible, use the same component from the main day view, just wrap it in a column and shrink its width" + follow-up "in multi-day 2, when i click on the task it should behave like the day view - it should open the edit modal."

- [chore]: **Deleted the Svelte multi-day view; the TS-rendered "multi-day 2" view is now the only multi-day view.** Removed `src/multiday/` (all `.svelte` components — `MultiDayApp`, `SummaryBar`, `InboxPanel`, `DayGrid`, `DayColumn`, `MiniTimeline`, `dragStore.ts`) and the V1 wrapper `src/multiDayView.ts`. Renamed `src/multiDay2View.ts` → `src/multiDayView.ts`; flipped the symbols inside (`VIEW_TYPE_MULTI_DAY_2` → `VIEW_TYPE_MULTI_DAY`, view type id `today-multi-day-2` → `today-multi-day`, class `MultiDay2View` → `MultiDayView`, display text `"Multi-day 2"` → `"Multi-day"`). Renamed every `dp-multiday2` / `dp-md2-*` / `--dp-md2-color` CSS class to `dp-multiday` / `dp-md-*` / `--dp-color` (the same custom property the daily view already uses, so the old V1 layered alias is no longer needed). Stripped the V1 `.dp-multiday`/`.dp-md-*` block (~450 lines) and the orphaned unlayered `.dp-md-timeline-block` override from `src/styles.src.css`. In `src/main.ts`: dropped the V2-only `open-multi-day-2` command and `activateMultiDay2View` method; the Mod+Shift+M hotkey still binds to `open-multi-day` (now resolves to the TS view). View type id changed, so any saved workspace tab pointing at the old `today-multi-day-2` will need to be reopened once. Files: removed `src/multiday/*`, `src/multiDayView.ts` (old), renamed `src/multiDay2View.ts` → `src/multiDayView.ts`; edited `src/main.ts`, `src/styles.src.css`. The user asked: "let's delete the svelte multi-day view."

- [fix]: Multi-day timeline blocks were escaping their day columns and rendering on top of adjacent days (e.g. "DGC choir" spanning Sun → Mon). Cause: the per-block-width pass I added earlier used `leftPct = idx * widthPct` where `widthPct` was computed from the block's own concurrency (`100 / peakConcurrent`). For a non-overlapping task that greedy placed in column 1, that meant `widthPct = 100` and `leftPct = 100` — the block started at the right edge of its day column and extended 100% wider, landing entirely inside the next day. Fix: rewrite to the standard Google-Calendar style — anchor `leftPct` to the cluster's column slot (`idx * (100 / colCount)`), and let each block extend right (`widthPct = ext * slotPct`) into adjacent columns whose blocks don't time-overlap this one. So a single non-overlapping task in a 2-column transitive group still renders full-width but stays inside its day; an overlapping pair splits 50/50; a 3-way overlap with the lane cap of 2 stacks the third on top of one of the lanes (cap behavior unchanged). Replaced `peakConcurrentDuring` with `rightExtensionCols`. File: `src/scheduler.ts`. The user reported: "somethings wrong - the tasks are escaping their day components."

- [feat]: New **Multi-day 2 view** — a TS-rendered alternative to the Svelte multi-day view, built directly with Obsidian DOM helpers and reusing the existing `multiDay.ts` data layer + `scheduler.ts` layout + `taskMove.ts` move helper. Layout: project-summary bar at top (hours per project, computed via `summarizeWindow`), inbox rail on the left with a search input, project-color filter chips, and a "Flat / Duration" toggle that switches between flat 28px rows and duration-proportional heights, and a 3-day timeline grid on the right. Tasks are draggable both ways: inbox → day timeline (snapped to `settings.snapMin`, writes `#t/HHpa`, optionally adopts subtask-sum duration on first schedule, then moves the line into the target day's daily note); day timeline → another day timeline (cross-file move); day timeline → inbox (strips `#t/`, sets a fresh `#o/` past the current max, moves into the inbox file). Drop indicator previews start/end clock times during drag. Wired as `VIEW_TYPE_MULTI_DAY_2` (`today-multi-day-2`) with the `calendar-days` icon. Command palette: **Open multi-day 2 view** (no default hotkey). Files: new `src/multiDay2View.ts`, `src/main.ts` (register view + activator + command), `src/styles.src.css` (~340-line `.dp-multiday2` block plus an unlayered `.dp-md2-timeline-block` button override mirroring the existing daily/multi-day pattern). The user asked: "let's create a new multi-day view, one not based on svelte. One based on the components you already have for the calendar. Design it like this: 1. project summary at the top - showing how many hours per project. 2. inbox on the left showing incompleted tasks. i can search + filter them. the height of these tasks can be flat or based on the duration of the task. These inbox tasks can be dragged onto the timelines. 3. The timelines are interactive - i can move tasks between the columns. It shows 3 days at a time." Follow-up: "add a command-pallete call for Multiday 2."

- [fix]: Multi-day timeline blocks were still rendering uniformly gray after the previous "add colors" pass. The CSS rule `.dp-md-timeline-block { background: var(--dp-color) }` lived inside `@layer components`, but Obsidian's default `button { background: var(--interactive-normal) }` is unlayered — and unlayered styles always beat layered ones regardless of specificity (same failure mode as the navbar-date-link bug fix). Fix: lift the `background-color` and `color` declarations out of the layer into the unlayered tail of `src/styles.src.css`, alongside the `.dp-datenav-label` overrides (the layered rule still sets the `--dp-color` custom property and other styling — only the actual paint moved out). Also switched the block label from vertically centered to **top-left** anchored (`display: block` + 3px top padding instead of `display: flex; align-items: center`) so a 30-min block reads more like a labeled tape than a centered button. Files: `src/styles.src.css`. The user reported: "i still don't see the colors on the calendar. you can put the text in the top left corner."

- [feat]: Multi-day timeline blocks simplified for legibility. Removed the in-block start-time annotation (and the verbose `9:30–10:00 · Title` tooltip — tooltip is now just the title); each block shows only the task title in a single line. Capped overlapping side-by-side lanes at **2 max** — added an optional `maxColumns` parameter to `layoutTimeline()` in `src/scheduler.ts` that, when the cap is hit, drops the new task into the column whose last block ends earliest (it visually overlaps but the lane count stays at 2 so blocks don't shrink past readability in 7-column mode). Calendar blocks now use the project color directly (dropped the `color-mix(... 92%, white)` lightening) so colors read more vividly; switched to a single-row centered layout matching the simpler title-only content. Files: `src/scheduler.ts` (new `maxColumns` param), `src/multiday/MiniTimeline.svelte` (passes `MAX_LANES = 2`, removes time span + verbose tooltip), `src/styles.src.css` (`.dp-md-timeline-block` background + flex layout). The user asked: "let's improve the visuals on the multi-day view. Let's remove the annotations. Only have up to 2 tasks side-by side for a given day. let's add colors to the calendar items. For visual simplicity, let's just show the task title."

- [feat]: New command **Open Today + multi-day + habits in popout** that creates a single native OS popout window with all three views laid out side-by-side as three vertical columns (Today | Multi-day | Habits). Uses `workspace.openPopoutLeaf()` for the first leaf, then `createLeafBySplit(leaf, "vertical")` twice to add the other two into the same popout. Mobile fall-back: opens each view as a regular tab since popouts are desktop-only. Files: `src/main.ts` (new `open-combined-popout` command + `openCombinedPopout` method). The user asked: "let's create a command that pops out the today, the multi-day, and the habits all at once in one window together."

- [feat]: Multi-day view header gets a **pop-out button** (`⤢` to the right of the `[3] [7]` toggle) that calls `app.workspace.moveLeafToPopout(this.leaf)` to detach the view into a native OS window. Threaded as a `popOut` callback prop from `MultiDayView` → `MultiDayApp.svelte` so the Svelte side never has to import Obsidian APIs directly. New `.dp-md-header-actions` flex group wraps the toggle + popout button, and `.dp-md-popout-btn` reuses the same border/hover styling as the nav buttons. Files: `src/multiDayView.ts` (new `popOut` prop), `src/multiday/MultiDayApp.svelte` (button + props update), `src/styles.src.css`. The user asked: "Add a button so I can pop-it out from obsidian."

- [feat]: Multi-day mini-timelines now match the daily-view style. Hour-gutter labels render as `formatClockShort(h*60)` ("9p" / "12p" / "7a") instead of raw 24-hour numbers; rows use the same dotted hairline lines; blocks render with the daily view's color-mixed background, accent stripe (`box-shadow: inset 2px 0 0 0`), rounded corners, and a two-line layout (start time on top in mono, title beneath). Overlapping tasks now lay out **side-by-side** via the existing `layoutTimeline()` from `src/scheduler.ts` — same column-packing the daily view uses — so a 3-day view with double-booked slots no longer shows blocks stacked on top of each other. Bumped px/min from 0.5 to 0.7 so short blocks stay legible. Files: `src/multiday/MiniTimeline.svelte` (uses `layoutTimeline` + `formatClockShort`, renders `dp-md-timeline-row` / `dp-md-timeline-line` / `dp-md-timeline-label` / `dp-md-timeline-blocks` mirroring the daily-view DOM, blocks render time + title rows), `src/styles.src.css` (rewrote the `.dp-md-timeline-*` block with a 26px gutter, mono 9px labels, and the `.dp-block`-style block treatment). The user asked: "style the timelines the same way the daily timeline is styled, with annotations and 9p on the timeline rather than 21. it can also have side-by-side plans on the 3-day view (rather than overlapping)."

- [fix]: Multi-day view rendered blank because svelte-preprocess (TypeScript transpiler) was elide-stripping the child `.svelte` component imports — TS sees them imported but doesn't see them used (template usage is invisible to the TS transpiler), so it dropped them, and esbuild then never bundled `SummaryBar`, `InboxPanel`, `DayGrid`, `DayColumn`, `MiniTimeline`. svelte-preprocess only auto-forces `verbatimModuleSyntax: true` when no tsconfig is present; ours was found, so it warned but didn't apply the option. Fix: pass `typescript.compilerOptions.verbatimModuleSyntax: true` directly to `sveltePreprocess()` in `esbuild.config.mjs`. Side cleanups: anchor → button on the day-column header (a11y), `svelte-ignore` for click-on-`<li>` warnings on draggable inbox/unscheduled items (intentional pattern). Also: added a default keyboard shortcut **Mod+Shift+M** to the "Open multi-day view" command. Files: `esbuild.config.mjs`, `src/main.ts` (hotkey), `src/multiday/DayColumn.svelte`, `src/multiday/InboxPanel.svelte`, `src/styles.src.css` (button reset on `.dp-md-day-link`). The user reported: "i can't see much! it's just a blank screen" and asked for a "pop-out hotkey".

- [feat]: New **Multi-day view** — separate ItemView showing 3 or 7 days side-by-side with a left inbox rail and a top project-color summary bar. Each day column has a clickable date header (opens the daily note), a compact mini-timeline showing scheduled tasks colored by project, and an unscheduled list beneath. Left/right arrows navigate by N days; a `[3] [7]` toggle in the header (persisted via the new `multiDayCount` setting, default 7) switches column count. Drag a task from the inbox onto a day column → moves the line out of `_inbox.md` and appends it to that day's daily note. Built with **Svelte 5** (`mount`/`unmount`, runes-based components) — first Svelte usage in this plugin. Files: new `src/multiDayView.ts` (ItemView shell mounting `MultiDayApp.svelte`), new `src/multiDay.ts` (data layer: `loadDayWindow`, `loadInboxTasks`, `summarizeWindow`, `buildWindowColorMap`), new `src/taskMove.ts` (extracts `moveTaskBetweenDailyNotes` from `view.ts`'s former `moveTaskWholeToDate` so both views share the line-move logic), new `src/multiday/{MultiDayApp,SummaryBar,InboxPanel,DayGrid,DayColumn,MiniTimeline}.svelte`, new `src/multiday/dragStore.ts` (module-scoped `DragState` slot for inbox→day handoff), new `src/svelte-shim.d.ts`, edits to `src/main.ts` (`registerView` + `open-multi-day` command + `calendar-range` ribbon icon + `activateMultiDayView`), `src/settings.ts` (new `multiDayCount` field), `src/styles.src.css` (~270-line `.dp-multiday` block with grid, mini-timeline, summary bar, drop-target highlights), `esbuild.config.mjs` + `tsconfig.json` (Svelte 5 + esbuild-svelte + svelte-preprocess wired in; bumped TypeScript 4.7 → 5.9 to satisfy svelte-preprocess; lib bumped to ES2020 for Svelte's typings). The user asked: "Similar to how you have the habits stats as a new window, I want a 'multi-day' view as a new window. … 7 days across … customizable, navigate left/right … summary at top of that week of summing the project colors and totalling the times … 3 or 7 days at a time … see my inbox on the left … tasks on the day appear either on the calendar, or underneath as unscheduled. Use Svelte to design this." Clarified: drag-to-schedule from inbox, mini timeline + unscheduled list per day, default 7 days, top stacked color bar + per-project minutes.

- [fix]: Hotkey-hints popup card was rendering with a transparent background on themes that ship a translucent `--background-primary` (Minimal, Things, Blue Topaz), so the timeline bled through. Switched the card to the same opaque-base trick `.dp-calendar` already uses — `background-color: var(--background-primary, #ffffff)` underneath a `linear-gradient(--color-paper, --color-paper)` overlay, plus `isolation: isolate`. File: `src/styles.src.css` (`.dp-pomo-hints.is-visible, .dp-timeline-hints.is-visible`). The user asked: "make the back of that modal not transparent. it's currently transparent."

- [feat]: Hotkey hints moved off the always-rendered hover bar onto a `?`-toggled popup. Press `?` to open a centered floating card listing every keyboard shortcut (timeline view or pomodoro view, whichever is active); press `?` again, `Escape`, the `×` close button, or click the dim backdrop to dismiss. The old bottom hint strip and its `:hover { opacity:1 }` reveal are gone (so are the `body.is-mobile` display-none overrides — the popup is keyboard-only and simply never opens on mobile). Files: `src/view.ts` (new `hintsVisible` field, `?` + `Escape` keybindings in `handleKeydown`, `renderTimelineHints` and the pomodoro hints block now early-return unless visible and wrap the list in a `.dp-hints-overlay` backdrop with a `.dp-hints-close` button; `?` itself listed as a hint), `src/styles.src.css` (replaced `.dp-pomo-hints` / `.dp-timeline-hints` hover styles with `.is-visible` floating-card styles plus `.dp-hints-overlay` backdrop and `.dp-hints-close` button styling). The user asked: "instead of the hover to show the hotkeys at the bottom, let's put it under a '?' - when ? is pressed, it pulls up the hotkeys and show a little UI."

- [feat]: Keyboard collapse shortcuts split — `s` collapses the summary stats row (unchanged), new `h` collapses the habits/quote/workout rows, new `b` toggles both at once. The navbar chevron now toggles "both" (matches `b`); icon flips to `chevron-down` only when both are collapsed. The previous `h → today` shortcut was removed (the sun-icon button still navigates to today). Files: `src/view.ts` (new `habitsCollapsed` field, new `toggleBothCollapsed` helper, key handler reshuffled, `renderSection` adds `is-habits-collapsed` class, chevron button rewired, hint legend updated), `src/styles.src.css` (`.dp-section.is-habits-collapsed` hides `.dp-quote-row`, `.dp-workout`, `.dp-habits`). The user asked: "Right now 's' collapses the summary. let's have 'h' collapse the habits and the quotes. and 'b' collapses both. The drop-down should toggle 'b'."

- [feat]: Intentions and quotes now render as inline Markdown — bold (`**foo**`), italic (`_foo_` / `*foo*`), inline code, and links all format properly instead of showing as raw syntax. Works for both the property-driven and `#intention` / `#quote` line-driven values. Implemented via Obsidian's `MarkdownRenderer.render` into the existing `.dp-intention` / `.dp-quote` spans, with the wrapper `<p>` Obsidian inserts unwrapped so the content still flows inline next to the rest of the header. Files: `src/view.ts` (`MarkdownRenderer` import, new `renderInlineMarkdown` helper on the view class, both intention + quote spans now fed through it instead of `text:`). The user asked: "Make sure that the quotes and intentions parse markdown. So if the property says 'Hello _i_ am here' it will respect that (or bold)."

- [feat]: Intention and Quote can now also live in the daily note's Obsidian frontmatter properties. The dashboard header looks up `intention:` / `quote:` (names match the configured tag) in the file's frontmatter first, and falls back to scanning for `#intention …` / `#quote …` lines if the property is absent or empty. String values, list values (first non-empty entry wins), numbers, and booleans are all supported. Settings descriptions for both tags rewritten to call out the property + hashtag pair and the priority. Files: `src/parser.ts` (new `parseFrontmatterField` helper alongside `parseTaggedLine`), `src/view.ts` (pulls `metadataCache.getFileCache(displayFile).frontmatter` and prefers it over the inline-tag scan for both intention and quote), `src/settings.ts` (Intention tag + Quote tag descriptions rewritten). The user asked: "While we can have #intention or #quote in the file itself, let's also pull from obsidian properties. By default, it should prioritize what's in the property over what's in the file body."

- [feat]: New **Templating → Stamp creation date in frontmatter tags** toggle (default on). When a daily note is created, a `<taskCreated>/YYYY-MM-DD` tag (e.g. `tcr/2026-05-09` — date is wall-clock today, prefix uses the configurable `taskCreated` setting) is injected into the YAML frontmatter `tags:` list. Handles all three frontmatter shapes — block list, inline `[a, b]`, bare scalar — and creates a brand-new frontmatter block when the template doesn't ship one. No-ops when the tag is already present so re-runs (or templates that already include it) don't duplicate. Wired through both `ensureDailyNote` and `applyDailyNoteTemplateIfEmpty`, so backfilled / wiki-link-created notes also get stamped. Files: `src/settings.ts` (new `addCreatedTagToFrontmatter` field + UI toggle in the Templating section), `src/dailyNote.ts` (new `addFrontmatterTag` helper, `addCreatedTag` on `DailyNoteFallback`, applied after `applyComputedParentDurations`), `src/main.ts` + `src/view.ts` (every fallback construction now passes `addCreatedTag`). The user asked: "Add as an option to the settings under templates to 'add #tcr/YYYY-MM-DD to the ---tags:--- property. It should be turned on by default. So when the new file is created, this tag is added.'"

- [feat]: Timeline blocks now render condensed on mobile — only the start time and the title — mirroring the existing overlap-mini treatment so long task titles stay visible. Project, tags, separators, the warn icon, and the context icon are hidden via CSS; the title is allowed to wrap (`white-space: normal; overflow-wrap: anywhere`) so it doesn't ellipse. Time switches to the `fmtClock(startMin)` short form (e.g. "4p") instead of the full "4p–5p (1h)" range. Files: `src/view.ts` (`renderBlock` adds `is-mobile-condensed` when `Platform.isMobile`; `compactTime` now also fires on mobile regardless of narrow/duration), `src/styles.src.css` (`.dp-block.is-mobile-condensed` rules — row layout, hide non-essential children, wrap the title). The user asked: "on mobile, I can't see the long task titles. So let's do what we do when there are multiple tasks overlapping. let's just show the start time and the title on mobile."

- [chore]: Center the quote line in the dashboard header (`text-align: center` on `.dp-quote-row`). The user asked: "make the quote centered."

- [feat]: New **Quote tag** mirroring the existing **Intention tag**. Anywhere `#quote ...` appears in the daily note, the rest of the line is parsed out and rendered on its own row in the dashboard header just below the intention. Pairs cleanly with the existing `<@quote>` template placeholder — putting `#quote <@quote>` in the daily-note template both seeds a random quote at note-creation time and surfaces it in the header. Tag is configurable (default `quote`); same matching rules as intention (lookahead guards `#quoted`/`#quote/foo`, first occurrence wins). Files: `src/parser.ts` (`parseIntention` renamed to `parseTaggedLine` since the implementation was already generic and now has two callers — comment + parameter name updated), `src/settings.ts` (new `quoteTag` field, default `"quote"`, **Tags → Quote tag** UI row beneath the intention row), `src/view.ts` (parses both intention and quote via `parseTaggedLine`, threads `quote` into `renderSection`, renders `.dp-quote-row` containing `.dp-quote` as a sibling of the subtitle so it lays out beneath it), `src/styles.src.css` (`.dp-quote-row` + `.dp-quote` styled italic muted to match `.dp-intention`). The user asked: "Same as you have 'intention', have something below it that's 'Quote'. This looks for #quote tags. This extends prior quote functionality, and let's me put #quote <@quote> to also show my quote in my plugin header."

- [feat]: New global command **Collect unfinished tasks into inbox** that sweeps `- [ ]` lines out of past daily notes (or today's, on demand) and re-files them into a configurable inbox file. The source line gets a new `- [>]` "migrated" state (distinct from `[x]` complete); a `- [ ]` copy is appended to the inbox carrying the same `#tid/<id>` so source and destination cross-reference. The command opens a scope picker (All past / Today) and, by default, a confirmation modal previewing the count and titles before writing. Subtree handling: when an unchecked parent is migrated, its unchecked descendants are migrated with it (indent rebased so the root sits at column 0); checked descendants stay behind. Bundled with a new `#tcr/<YYYY-MM-DD>` creation-date tag (configurable prefix `taskCreated`, default `tcr`) auto-stamped by the new-task modal so future migrated copies carry the date the task was originally made; tasks lacking the tag at migrate time get one stamped onto the inbox copy with the source daily note's date. Two new settings under **Tasks → Inbox**: "Inbox file path" (default `{daily}/_inbox.md`; `{daily}` resolves to the active daily-notes folder) and "Confirm before migrating" (default on). Files: `src/parser.ts` (`taskCreated` added to `TagPrefixes` + `DEFAULT_PREFIXES`; `buildTagRegexes` extended with the date-shaped `taskCreated` regex; new `setTaskMigrated`, `setTaskCreatedTag`, `parseTaskCreated` helpers; `setTaskTitle`'s tag-preserving alternation now includes `taskCreated`), `src/dailyNote.ts` (new `getDailyNoteOptions`, `parseDailyNoteDateStr`, `listDailyNotes`, `todayDateStr` helpers), `src/settings.ts` (`inboxPath` + `confirmCollectMigration` fields, defaults, Tasks-tab UI rows for both plus the new `#tcr/` prefix; `taskCreated` added to the `hide()` migration key list), `src/migrate.ts` (`keyLabel` extended for `actual`/`taskContext`/`taskCreated`), `src/view.ts` (new-task modal `onSave` now stamps `#tcr/<today>` after context tags), new `src/collect.ts` (sweep entry point, scope/confirm modals, `scanContent` + `applyPlan` logic), `src/main.ts` (registers the `collect-unfinished` command). The user asked: "let's also have a global command 'collect unfinished' into an inbox. … by default `{daily}/_inbox.md` … When a task is migrated, a duplicate is formed and a taskid is generated. The task is no marked complete, but `- [>]` for 'migrated'. Under tasks, have symbols for `[x]` complete and `[>]` migrated. Tasks will be copied from their daily file to the _inbox. the daily file gets `[>]` and the inbox will get a `[ ]` copy of the task. they'll share a task-id." Followups clarified: scope is asked at command time (All past default, Today optional); parent + all unchecked children migrate together; scheduled `#t/` tasks are swept normally; confirmation toggleable in settings; inbox is a flat list with no headings; each migrated task carries `#ta/YYYY-MM-DD` — collision flagged with the existing actual-duration `#ta/`, so the new prefix is `tcr` (separate setting); auto-stamp only on the plugin's new-task modal. The earlier "move completed tasks to bottom of heap" idea was dropped.

- [fix]: Navbar date title link was still rendering blue/underlined despite the previous override. Cause: the override sat inside `@layer components`, but Obsidian's default `a { color: var(--text-accent); }` is unlayered, and unlayered styles always beat layered ones regardless of selector specificity. Moved the override out of the layer (placed alongside the existing unlayered `.dnd-poly-drag-image` rules at the bottom of `src/styles.src.css`) so it actually wins the cascade. Now: `--color-ink` color, no underline, `cursor: pointer`, `opacity: 0.75` on hover.

- [chore]: Navbar date title link no longer turns blue or gets underlined on hover — keeps `--color-ink` color and no underline in all states (base / hover / focus / active), with a subtle `opacity: 0.75` on hover as the only affordance. File: `src/styles.src.css` (`a.dp-datenav-label.is-clickable` rule). The user asked: "update the formatting so it's not blue and not underlined".

- [feat]: Navbar date title (e.g. "Sat, May 9, 2026") is now clickable and opens the displayed daily note; the redundant `daily/YYYY-MM-DD.md` path subtitle below the date has been removed. Files: `src/view.ts` (`renderDateNav` takes a `displayFile` and renders an `<a class="dp-datenav-label is-clickable">` that calls `openFile`; primary `renderSection` call passes `""` for subtitle so the path link is dropped), `src/styles.src.css` (hover/cursor styles for `a.dp-datenav-label.is-clickable`). The user asked: "update the title on the navbar of the plugin to be clickable. Right now it says Sat, May 9, 2026. When i click this, it should open the file. This means you can get rid of the daily/YYYY-MM-DD.md on the second line".

- [feat]: Note strips on the timeline are now clickable like regular calendar blocks. Clicking the title toggles the task's checked state (strikethrough via the existing `.dp-note-strip.is-done` styling); clicking elsewhere on the strip (dot, time, blank space) still opens the task editor. File: `src/view.ts` (`renderNoteStrip` — added a title-only click handler that calls `applyLineChecked` and stops propagation; the strip-level handler that opens the editor is unchanged). The user asked: "can you make notes clickable? when #note is on, it creates an annotation. I want this clickable (to check the task). when the title is clicked, it creates a strikethrough (just like for a normal calendar item)".

- [chore]: The settings-tab intro ("Today plugin settings" + description blurb) is now collapsed by default — the title doubles as a `<details>` summary with a chevron, so the description only shows when expanded. Files: `src/settings.ts` (`renderIntro` switched from `<div>`/`<h2>`/`<p>` to `<details>`/`<summary>`/`<p>`), `src/styles.src.css` (chevron + open/closed states for `.dp-settings-intro-title`).

- [feat]: New `<@quote>` template placeholder + **Templating → Quotes file** setting. The quotes file (default `daily/_quotes.md`) holds one quote per line — each carriage return starts a new quote — and the placeholder is replaced with a randomly chosen line at note-creation time. Empty/missing path or empty file → placeholder collapses to "". Threaded through `DailyNoteFallback` (`quotesFile?: string`) and applied in both `ensureDailyNote` and `applyDailyNoteTemplateIfEmpty` so backfilled / wiki-link-created notes also get a quote. Added a "Template placeholders" intro block to the Templating settings section explaining `<@today>`, `<@yesterday>`, `<@tomorrow>`, `<@Nd>`, the `-rel` suffix (anchors to the file's own date instead of wall-clock today), and the new `<@quote>`. Files: `src/settings.ts` (new field + UI + intro), `src/dailyNote.ts` (`quotesFile` on `DailyNoteFallback`, new `pickRandomQuote` helper, `expandDateTemplate` extended with a `quote` parameter that handles `<@quote>` before falling through to the date-keyword branch), `src/main.ts` + `src/view.ts` + `src/habitsView.ts` (every fallback construction now passes `quotesFile`). The user asked: "Let's have a quotebank to put an inspiring quote at the top of the created file. … a carriage return creates a quote. … picked at random. update the templating documentation to explain @today-rel and @quote".

- [feat]: Parent task duration can now be derived from subtasks. When a parent task has no `#d/` tag but its subtasks carry `#d/` tags, the parent adopts the sum of the subtask durations (subtasks without a `#d/` are skipped). Triggers in three spots: (1) when a daily note is first created from a template (both the explicit `ensureDailyNote` path and the empty-file backstop in `applyDailyNoteTemplateIfEmpty`), (2) when a new task is saved via the new-task modal and the user didn't touch the duration field, and (3) when an unscheduled card without an explicit duration is dropped onto the timeline — the drop now writes `#d/<sum>` onto the parent in the same edit that sets `#t/`. Once written, the parent has an explicit duration so the computation never runs again. Files: `src/parser.ts` (new `sumSubtaskDurations` and `applyComputedParentDurations` helpers), `src/dailyNote.ts` (`prefixes` added to `DailyNoteFallback`, helper applied to expanded content before vault write/modify), `src/main.ts` + `src/view.ts` (every fallback construction now passes `prefixes`), `src/view.ts` (`TaskEditModal.onSave` now passes `null` for `durationMin` whenever the user didn't change the field — including new-mode — so the modal-save handler can detect "untouched" and prefer the subtask sum; `DragPayload` extended with `hasExplicitDuration` + `subtaskRawLines`; `handleDropOnTimeline` writes the sum on unscheduled drops). The user asked: "if a duration for the task isn't specified, then it can be computed by default by the length of the subtasks" — clarified to use the system's `#d/` tag (skip subtasks missing it), apply at template/modal-save/unscheduled-drop, and write the sum onto the parent so it sticks.

- [feat]: Habit log tags now use the short shape `#h/<slug>` (matching exercise sets) instead of `#h-<period>/<slug>`. Goal lines in `_habits.md` keep the `-day`/`-week`/`-month` segment — the period segment only ever encoded *which goal* the log belonged to, but with logs scoped by the daily note's date window the period is implicit, so dropping it makes the log grammar identical to `#x/<name>/N`. The trailing `/N` count is now honored: `- [x] drink #h/drink/3` contributes 3 toward the goal in one line (decimals via `_`, e.g. `#h/drink/2_5` = 2.5). Goal targets accept the same decimal grammar (`#h-day/drink/4` = 4 glasses, `#h-day/run/1_5` = 1.5 mi). The find/count regex tolerates both `#h-<period>/<slug>` and `#h/<slug>` so unmigrated notes still tally correctly while the user does a manual find/replace pass on legacy log tags. Files: `src/habits.ts` (new `buildHabitLogTagRegex`; `findHabitTaskLines` returns `count` summed from `/N` segments and no longer takes `period`; `appendHabitLine`/`toggleHabitOnContent` drop period; `parseHabitsFile` accepts decimal targets), `src/view.ts` + `src/habitsView.ts` (call sites updated, sums use `l.count`; `applyHabitToggle` drops the unused period arg), `src/settings.ts` (settings copy refreshed for the new shape). The user asked: "habits and exercises are logging differently. The habit goals are #h-day/friend and the logging also is #h-day/friend, but #x-day/Push-ups is logged as #x/day/Push-ups. I want the habit logging to happen the same way the workout logging happens, so the #h-day/friend goal is logged as #h/friend. and there could even be #h/friend/2 if i did the habit 2x". On follow-ups: toggle still flips the checkbox / appends a single bare-tag line (user types `/N` or extra lines manually); logs map to goals via the daily note's date window (so a `#h/eatout` log can count toward both daily and weekly `eatout` goals if both exist); decimals allowed on both counts and targets. An auto-migration scoped to daily-note files was added then removed at the user's request — they preferred a manual rewrite over permanent migration code; the read regex's both-format tolerance covers the transition.

- [feat]: Per-weekday daily-note templates. New **Templating** settings tab (split out of the Automations tab) holds the existing daily-note format / folder / template fields plus seven new file pickers (Sunday → Saturday). When a daily note is created, the matching weekday template's contents are appended to the base **Daily note template** (separated by a blank line); empty rows skip cleanly so the base template still applies. New `dailyNoteTemplatesByDay` setting (`DailyNoteWeekdayTemplates` shape, all empty by default), threaded through `DailyNoteFallback` and into a new `readCombinedTemplate` helper that picks the day file via `Date.getDay()` against `WEEKDAY_NAMES`. `applyDailyNoteTemplateIfEmpty` parses the date out of the filename for the day-of-week lookup so backstop-applied templates (e.g. when Obsidian creates a future-dated empty file from a wiki link) get the right weekday append. Files: `src/settings.ts` (interface, defaults, new tab + section, `WEEKDAY_NAMES`), `src/dailyNote.ts` (`templatesByDay` on `DailyNoteFallback`, `readCombinedTemplate`, both create paths use it), `src/main.ts` (loadSettings deep-merges the new sub-object; both fallback constructions pass it), `src/view.ts` + `src/habitsView.ts` (every fallback construction now includes `templatesByDay`). The user asked: "add a new functionality to create daily-specific templates. These won't override, but add to the base template. … 8 template folders: 1. day.md (current) 2. monday.md … 8. saturday.md. The template resolver will join them together by just appending the day's template to the day.md template".

- [feat]: Exercise tags now accept `_` as a decimal separator inside numeric segments, since Obsidian tags can't carry a literal `.`. So `#x/Run/1_5` parses as 1.5 reps and `#x/BodyWeight/189_3` as 189.3. Applies to both the reps slot and the optional weight slot, e.g. `#x/bench/8/162_5`. Same rule extended to the exercise-goal targets in `_habits.md` (`#x-day/Run/1_5`). Files: `src/parser.ts` (exercise regex broadened to `\d+(?:[._]\d+)?` for both slots; `parseExercises` switched to `parseFloat` and replaces `_`→`.` before parsing; reps now stored as `number` w/ decimal support), `src/habits.ts` (`parseExerciseGoals` regex + parse mirror the same rule). The user asked: "when parsing numbers, treat _ as a dot. so x/Run/1_5 is 1.5 or x/BodyWeight/189_3 is 189.3".

## 2026-05-08

- [feat]: New-task modal title input now offers an autocomplete on prior tasks across the vault. Typing "Dri" surfaces past tasks like "Drive home" with their project shown muted on the right; picking one (mouse / Enter / Tab) pre-fills title, project, duration, description, and `#tc/` tags from the source task. Sub-tasks are gated by a new **Tasks → Defaults → Copy sub-tasks on title autocomplete** toggle (default off) — when on, the source task's checklist is copied as a fresh, unchecked list. The popover defers to the existing `##` / `#@` / `#$` / `@` trigger autocompletes by hiding whenever any trigger character is in the title, and uses capture-phase keydown handling so picks don't double-fire with the modal's submit. Suggestions are deduped by title (case-insensitive, most-recent-modified file wins) and loaded via `vault.cachedRead` so repeat opens are cheap. Files: `src/view.ts` (new `TaskSuggestion` type, `collectPriorTaskSuggestions` method on `TodayView`, `getPriorTasks` + `copySubtasksOnAutocomplete` on `TaskEditOpts`, popover wiring inside `TaskEditModal.onOpen`, `openNewTaskModal` passes through), `src/settings.ts` (`copySubtasksOnAutocomplete` field + Tasks-tab toggle). The user asked: "When i'm typing in the edit task modal, let it bring up old tasks to be able to copy them. … fuzzy-search on old tasks so i can copy old tasks. It won't copy subtasks by default (but make this configurable)".

## 2026-05-07

- [fix]: Sub-task delete control rendered as a blank gray square because the underlying `<button>` was picking up Obsidian's default modal button styling, which boxed out the X icon. Switched it to a `<span role="button" tabindex="0">` (mirroring the drag handle), so the lucide `x` icon now shows. Files: `src/view.ts` (`renderSubtask` element type), `src/styles.src.css` (simplified `.dp-edit-subtask-delete` — removed button-resets that were no longer needed). The user reported: "the delete button icon isn't showing up well - can you make it an x icon?".
- [chore]: Settings folder inputs now have a folder-picker dropdown — typing into **People folder** or **Daily notes folder** filters all vault folders by path-substring and clicking one fills the input. Built a new `FolderSuggest` (mirror of `FileSuggest`) using `AbstractInputSuggest<TFolder>` that walks the vault root, skips the unnamed root entry, and renders folder name + parent path. Files: `src/settings.ts` (new `FolderSuggest`, wired into both folder fields). The user asked: "make sure the config for 'people' searches for files. right now it's just a text inbox".
- [feat]: Edit modal sub-task rows: added a small `×` delete button (just before the drag handle) that removes the sub-task. Splices the file line via a new `applyDeleteSubtask` method and a new `onDeleteSubtask` opts callback; in-memory `subs` are spliced and remaining real-file `lineNumber`s decremented so subsequent edits in the same modal session still target the right lines. New-mode (negative line numbers) subs are dropped from the in-memory list only — they never made it to disk. After delete the list re-renders, which also recomputes the running total. No confirm prompt — sub-tasks are small enough to retype, and the icon is small/contained enough that misclicks are rare. Files: `src/view.ts` (`onDeleteSubtask` opts type, `applyDeleteSubtask` method, button + click handler in `renderSubtask`, `openTaskEditor` wiring), `src/styles.src.css` (`.dp-edit-subtask-delete` icon button with red-on-hover). The user asked: "add the ability to delete a subtask".
- [feat]: Edit modal sub-task header: appended a running total `(3h15m)` next to the "Sub-tasks" label, summing each sub's `#d/` duration. Goes red+bold when the sum exceeds the parent task's selected duration so the over-budget signal is hard to miss. Updates live: when a sub-task is added, when its duration chip is changed, when its inline text is edited (in case the user types `#d/` directly), and when the *parent* duration changes (chip click, custom input, autocomplete commit) so the comparison stays correct in both directions. Implemented as a `subTotal` span inside the existing label and an `updateSubTotal()` closure forward-declared so the duration-row handlers can call it before the sub-task header is built. Files: `src/view.ts` (forward-declared `updateSubTotal`, header span, recompute hooks in renderList/duration-chip/text-edit/add/parent-duration changes), `src/styles.src.css` (`.dp-edit-subtask-total`, `.is-over` red variant). The user asked: "can you sum the duration by the subtasks. so 'Subtasks (3h15m)' by the subtask title? then put it in red if it's more than the duration of the parent task?".
- [feat]: Edit modal sub-task rows: added a duration chip (`+ dur` / `30m` / `1h30m`) sitting right after the time chip. Click to edit inline (parses the same compact format as the parent duration field, accepts a leading `#d/` paste); Enter writes the `#d/` tag into the sub-task line via `setDurationTag`/`removeDurationTag`. New `onSetSubtaskDuration` callback on `TaskEditModalOpts`, wired to a new `applySubtaskDuration` method on the view. Files: `src/parser.ts` (new `removeDurationTag` mirroring `removeTimeTag`), `src/view.ts` (`renderSubtask` adds the chip and click→inline-editor flow, opts add `onSetSubtaskDuration`, `openTaskEditor` passes it through, `applySubtaskDuration` writes through `app.vault.process`), `src/styles.src.css` (`.dp-edit-subtask-duration` mirroring the time-chip pill in a neutral ink palette so the two chips read distinctly). The user asked: "can you add a duration on subtasks? right next to the time button on the subtasks?".
- [feat]: Edit modal sub-task list: ArrowUp/ArrowDown now navigate between sub-task text fields. Pressing ArrowUp inside the empty new-sub input (Slack-style) drops into edit mode on the most recent sub-task; ArrowUp inside a sub-task text editor moves to the previous sub-task; ArrowDown moves to the next, or focuses the new-sub input if already on the last. Implemented by extracting a `startTextEditAt(idx)` helper that programmatically clicks the row's text element to reuse the existing inline-editor flow (which auto-commits on the way out via `finish(true)`). Files: `src/view.ts` (helper + new keydown branches on the text editor and `addInput`). The user asked: "when i'm adding a new subtask, and i hit the 'up' arrow, can it go up to the prior subtask to let me edit it? … if i hit 'up' twice, it goes to the next subtask, in edit mode. 'down' also works".
- [feat]: `@`-trigger autocomplete now mixes "people" alongside the date suggestions. New **People folder** setting in the Autocomplete tab points at a vault folder of one-file-per-person notes (e.g. `people/`); when set, every basename in that folder shows up under the same `@` picker as `today`/`tomorrow`/`Nd`, filtered by the typed query (so `@bob` surfaces every Bob). Picking one inserts a wikilink to that note (or markdown link if the vault is configured for them). Works in the new/edit task modal title input and in the editor (`InlineSuggest`) — same trigger string, same picker, just two pools merged. Empty setting disables people lookup so existing behavior is preserved. Files: `src/people.ts` (new module: `buildPeopleSuggestions`, `buildPersonLinkInsert`), `src/settings.ts` (`peopleFolder` field + UI), `src/main.ts` (`InlineSuggest` date branch mixes people), `src/view.ts` (`TaskEditOpts.peopleFolder`, modal date rule mixes people, both call sites pass it through). The user asked: "add an auto-complete to reference people. People will be files in a particular folder (/people) … when I do `@`, it should also show people. So @bob will pull up all my Bobs. This auto-complete should work in the task title as well as the file".
- [chore]: Edit/new task modal: tag chips moved out of the input field and now render as a small chip strip directly below the tag input (smaller font + tighter padding), so the input itself reads as a normal text field aligned with the project input on the same row. Empty state hides the chip row entirely so an unused tag picker doesn't reserve vertical space. `.dp-project-row` switched from `align-items: stretch` to `flex-start` so the project input keeps its natural height when the tag column grows. Files: `src/view.ts` (modal layout: `dp-tags-chips` is a sibling of `tagInput`, not its parent; removed the chip-strip click-to-focus handler since the input is now its own field), `src/styles.src.css` (rewrote `.dp-tags-input-wrap` to column-flex; new `.dp-tags-chips`; smaller `.dp-tag-chip*`; `.dp-project-row` align change). The user asked: "show the tags below the tag box on the edit task modal. make them small".
- [fix/feat]: Calendar block resize on mobile only advanced one snap step before the gesture stopped — the `.dp-resize-handle` had no `touch-action`, so after the first move the browser hijacked the vertical drag as a scroll on the timeline pane and cancelled pointer capture. Added `touch-action: none` so the handle owns the gesture for its full length. Also added a top resize handle (`.dp-resize-handle-top`) on every block with an explicit start time: dragging it anchors the end time and shifts both start (`#t/`) and duration (`#d/`) in lockstep, snapping to the configured interval and clamping at the snap minimum / midnight. Files: `src/view.ts` (top handle wiring in `renderBlock`, new `beginResizeTop` + `applyStartAndDurationChange`), `src/styles.src.css` (`touch-action: none` on the base handle, `.dp-resize-handle-top` variant). The user asked: "on mobile, when i click and drag down the task on the calendar (to expand it), it only lets me drag one 15m block at a time … on both desktop and mobile i'd like to be able to 'drag up', which means if i put my mouse/finger on the top border of the calendar item i could be able to drag the start time earlier".
- [feat]: Task edit/new modal now has a tag picker sitting to the right of the project dropdown, so `#tc/<slug>` labels are managed visually instead of having to be typed inline. Existing tags surface as removable chips, an inline input filters/picks from suggestions sourced via a new `collectContextTagNames()` (mirror of `collectProjectNames()` reading the metadata cache), and Enter on a free-form value adds a new label. On save, the modal compares the resulting list to `initialTags` and routes through a new `setTaskContextTags(rawLine, tags[], prefixes)` parser helper that strips every existing `#tc/` and re-appends the chosen ones; null = unchanged. New `initialTags` and `availableTags` opts on `TaskEditOpts`, and the `onSave` signature gained a `tags` arg before `extras`. Files: `src/parser.ts` (`setTaskContextTags`), `src/view.ts` (modal opts, picker UI inside `.dp-project-row`, `resolveTags()`, `collectContextTagNames`, `applyTaskEdit` accepts/applies tags, new-task path also routes tags through `setTaskContextTags`), `src/styles.src.css` (`.dp-tags-input-wrap`, `.dp-tags-input`, `.dp-tag-chip*`, `.dp-tag-input`). The user asked: "context tags should be visible inside the edit modal frame. i should be able to add more tc tags. Put it to the right of the projects dropdown. let me choose multiple tags (each one adding to the task line)".
- [feat]: Project summary now has a squares-grid visual under the table, mirroring the day-grid box shown under the time summary. Each project contributes one 6×6 colored square per 15 minutes (`Math.round(total / 15)`, min 1 square if any time logged), drawn in the same descending-by-total order as the rows above and tinted with the project's color from `colorMap`. Unassigned minutes get hatched squares using the same diagonal pattern as the swatch. Total square count is unbounded — overlapping or back-to-back tasks for the same project compound naturally, so a day with several parallel time entries can exceed the 96 dots that the time grid uses. Files: `src/view.ts` (`renderProjectTable` now wraps its table in a `dp-stat-col` and calls a new `renderProjectDotGrid`), `src/styles.src.css` (`.dp-day-dot-unassigned` hatched variant). The user asked: "underneath the project summary, can you have a squares visual (similar to the scheduled box) that shows one box per 15m of each project i've worked on? this may be more than 96 since there can me multiple time entries per hour".
- [feat]: Tasks can now carry free-form context labels via a new `#tc/<slug>` tag (e.g. `#tc/billable`, `#tc/client-acme`). Multiple tags per task are allowed; they're parsed onto `ParsedTask.tags` and rendered as small pill chips next to the project label on both timeline blocks and unscheduled cards. New `Task context tag prefix` setting in the Tasks tab lets the user rename the prefix, and the existing prefix-migration flow picks the new key up automatically. Files: `src/parser.ts` (`TagPrefixes.taskContext`, `DEFAULT_PREFIXES.taskContext = "tc"`, regex in `buildTagRegexes`, `ParsedTask.tags`, `parseTaskContexts`, populated in `parseTaskLine`, added to `setTaskTitle`/`setTaskDescription` tag boundary), `src/view.ts` (`cleanBody` strips `#tc/`, render `dp-block-tags`/`dp-card-tags` blocks after the project label), `src/settings.ts` (new prefix Setting in `renderTaskDefaultsSection`, `taskContext` added to migration keys), `src/styles.src.css` (`.dp-block-tags*`, `.dp-card-tags*` pill chips). The user asked: "can you add tags to the tasks? these will all be under #tc - task context. put it next to projects. By putting in tags, I can label things (#tc/billable)".
- [fix]: Narrow timeline blocks (≤15m mini and 16–30m two-line) had the title text element stretching across the rest of the row via `flex: 1 1 auto`, so clicks anywhere on the empty horizontal space after the visible title still hit `.dp-block-text` and toggled the task. Switched to `flex: 0 1 auto` (matching the non-narrow rule) so the title's hit-area sizes to the rendered text and still ellipses when too long. Files: `src/styles.src.css` (`.dp-block.is-narrow-mini .dp-block-text`, `.dp-block.is-narrow-2line .dp-block-text`). The user reported: "the 15m tasks are clickable all across the top border. perhaps because of recent changes to how the rendering works. I still only want the task text to be clickable".
- [feat]: Added three new command-palette commands — **Open today's daily note**, **Open yesterday's daily note**, **Open tomorrow's daily note** — that resolve the daily note for the matching date (creating it from the configured template if it doesn't yet exist) and open it in the active leaf. Files: `src/main.ts` (new `openDailyNoteForOffset` helper, three `addCommand` registrations, `ensureDailyNote` import). The user asked: "can you add a command pallete command for going to today/yesterday/tomorrow's notes".
- [feat]: Habit stats pane: added top-of-pane tabs **Habits** / **Workouts**. Habits tab is the existing Day/Week/Month heatmap stack; Workouts tab is a per-day rep log shaped `| exercise | day1 | day2 … | total |` over the same `habitsStatsWindow` of days, with rows sorted by total reps descending and only counting completed (`- [x]`) sets. Files: `src/habitsView.ts` (`StatsTab` field, `renderTabs`, `renderWorkoutLog`), `src/styles.src.css` (`.dp-habit-stats-tab*`, `.dp-workout-log*`). The user asked: "on the habits stats table, can you show me a tab for workouts? this will have a log of how many pushups I did each day. so the table is | exercise | date1 | date2 | date3 - showing counts for each day".
- [fix]: Habit stats totals (`675 reps • Push-ups 250 · …`) now only sum completed (`- [x]`) exercise sets. Previously they summed every set on every daily note in the window, so future days that already had `#x/…` tags planned but unchecked inflated the totals with intent rather than work done. Files: `src/habitsView.ts` (`buildSection` skips `!set.done` when accumulating `totalReps`/`exerciseTotals`). The user noticed: "the habits stats summary should only be summing exercises where - [x]. i think it's counting future days that haven't yet been logged".
- [fix]: Narrow timeline blocks now adapt the header to how much vertical room they actually have. Previously a 15m overlapping block showed the 3-line stack (icons-time / project / title), but the block is only ~18px tall and the parent's `align-items: center` clipped the top so the title disappeared. Now: ≤15m narrow → single condensed line `4p Run errands` (project + duration hidden, title ellipses but still toggles); 16–30m narrow → two lines `4p Run errands` / `proj/sub` (CSS `order` puts the title next to the time and forces the project to wrap to a new line); >30m narrow keeps the existing 3-line stack. The time string is shortened to just the start time (e.g. `4p`) for ≤30m narrow blocks, and stays as the full `4p–4:15p (15m)` for wider/wide blocks. Files: `src/view.ts` (`renderBlock` adds `is-narrow-mini`/`is-narrow-2line` classes + compact time text), `src/styles.src.css` (mode-specific row layouts; existing 3-line stack scoped to `.is-narrow:not(.is-narrow-mini):not(.is-narrow-2line)`). The user asked: "if it's a 15m meeting, it hides the top because it's centering the text. … let's just show the start time (5p) and title. that's just for the 15m or 30m wrapped. Project can be on 2nd line for the 30m … like `4p Run errands` - so i can still toggle the task on/off".
- [feat]: Timeline blocks: when 2+ events overlap and share a time slot horizontally (each block ≤ 50% of the lane width), the header trio now stacks onto multiple lines — `[icons] time` / `project/sub` / `task title` — instead of trying to fit "(2h 15m) · proj/sub · long task title" on one ellipsed row. Added an `is-narrow` class on the block when `widthPct < 99.5`, wrapped the existing context-icon + warn + time spans in a `.dp-block-meta` container so they stay together as the first stacked line, and switched the narrow row to `flex-direction: column` with separators hidden and title text allowed to wrap. Files: `src/view.ts` (`renderBlock` adds `is-narrow` class + `.dp-block-meta` wrapper), `src/styles.src.css` (`.dp-block-meta`, `.dp-block.is-narrow .dp-block-row/.dp-block-sep/.dp-block-text/.dp-block-project-wrap`). The user asked: "When there are two calendars at the same time, can you update it so that it line breaks? e.g., time\\nproj\\ntask?".
- [chore]: Settings: rename the "Hotkeys & Defaults" tab to "Automations". Files: `src/settings.ts` (`TAB_SPECS.general.label`). The user asked: "rename 'hotkeys & Defaults' to 'Automations'".
- [feat]: Settings: new **Day** tab (`sun` icon) holding the work-hours and wake/sleep settings, pulled out of View. View now contains just timeline rendering (pixels per minute, visible start/end hour, timeline heights). Files: `src/settings.ts` (`SettingsTab` adds `day`, `TAB_SPECS` entry, `renderDaySection`, `renderViewSection` slimmed). The user asked: "add a new 'day' config, this is the work day start/end and sleep. - i mean a new day tab, not config".
- [feat]: Habits dashboard: a weekly/monthly habit completed on a specific day now strikes through only on that day's view and disappears entirely from other days in the same window. Previously a Mon-checked weekly habit showed as completed (struck through) on Mon, Tue, Wed, …; now Tue's view doesn't list it at all. Reduces visual noise on days where the user didn't contribute to a given habit. Daily habits unaffected (their window is the displayed day). Implemented by adding `checkedOnDisplayedDate` to the per-habit display state and filtering on `isComplete && !checkedOnDisplayedDate`. Settings toggle "Hide completed habits" still hides every completed habit including the day it was checked. Files: `src/view.ts` (`HabitDisplay.checkedOnDisplayedDate`, `loadHabitDisplays`, visibility filter in `renderHabitsLine`), `src/settings.ts` (toggle copy). The user asked: "if i complete a habit today, cross it off on the summary bar. if i completed a task on another day, then don't show the habit (reduce the visual complexity). so a habit completed on may 5 will appear as crossed out on the may 5th summary view, but not on the may 6th summary view".
- [feat]: Settings: added two new tabs — **Tasks** (`list-checks`) and **View** (`eye`). Tasks holds the Defaults section (default duration, quick-duration chips, snap interval, duration/time/order/exercise tag prefixes), Task ID, and Notes (note + intention tags). View holds Pixels per minute (moved out of Defaults) plus the Day-config block (visible/work/wake hours + timeline heights). Hotkeys & Defaults is now just Autocomplete + Templating. Files: `src/settings.ts` (`SettingsTab` extended w/ `tasks`/`view`, `TAB_SPECS` entries, dispatch, `renderDefaultsSection` → `renderTaskDefaultsSection` w/o pxPerMin, `renderDayConfigSection` → `renderViewSection` w/ pxPerMin at top). The user asked: "let's create a 'Tasks' tab. this has the settings about task id, also the duration, the snap interval, etc. … Then have a 'view' tab - which relates to the height/timeline choices".
- [fix]: Settings: bumped the divider under the tab chips to 2px and dropped the redundant border-bottom on the intro block so the tab strip reads clearly as nav. Files: `src/styles.src.css` (`.dp-settings-tabs` border-bottom 1px → 2px, `.dp-settings-intro` no longer draws a border). The user asked: "have a line underneath the tab chips so it's easy to see it's a setting".
- [feat]: Habit tag syntax migrated from `#h/<period>/<slug>` to `#h-<period>/<slug>[/<target>]`, mirroring the exercise-goal shape (`#x-<period>/<name>/<target>`). The optional trailing `/N` sets a per-period target — `#h-week/laundry/2` means "twice per week"; the dashboard chip shows `laundry 1/2` in progress and the stats heatmap colors weekly cells met/not-met against the target (target=1 habits keep the count-shaded heatmap). Toggling habits and the per-file duplicate warning still work as before; the duplicate warning is suppressed for target>1 habits since multiple checks per file are legitimate progress. Migrated all `#h/day/`, `#h/week/`, `#h/month/` occurrences in the vault. Files: `src/habits.ts` (`Habit.target`, regex, `parseHabitsFile`, `appendHabitLine`), `src/view.ts` (`HabitDisplay.checkedCount`, chip progress span), `src/habitsView.ts` (heatmap intensity + label target), `src/settings.ts` (copy update), `src/styles.src.css` (`.dp-habit-progress`). The user asked: "I want to improve the habits — maybe i want something done N times a day. let's do B, migrate to h-week h-day syntax. and keep x-day".
- [feat]: Settings: added an intro block above the tab bar (display-font title + muted blurb with a README link) and Lucide icons on each tab — `sliders-horizontal` for Hotkeys & Defaults, `folder-kanban` for Projects, `timer` for Pomodoro, `repeat` for Habits. Active tab tints both the chip and the icon with the accent color. Files: `src/settings.ts` (`renderIntro`, `TAB_SPECS` w/ icons, `setIcon` import), `src/styles.src.css` (`.dp-settings-intro*`, `.dp-settings-tab-icon`, `.dp-settings-tab-label`). The user asked: "can you have an intro text above the tabs? can you style the tabs with lucide-react icons?".
- [feat]: Settings UI organized into 4 tabs — Hotkeys & Defaults, Projects, Pomodoro, Habits — with a tab bar at the top of the settings pane. Switching tabs swaps the body. "Hotkeys & Defaults" gathers Defaults, Task ID, Autocomplete, Notes, Templating, and Day-config sections; "Projects" gathers Projects + Context tags; the other two tabs each hold their single existing section. Files: `src/settings.ts` (new `SettingsTab` type, `activeTab` state, `renderTabs`, dispatch in `display()`), `src/styles.src.css` (`.dp-settings-tabs`, `.dp-settings-tab`, `.is-active`, `.dp-settings-pane`). The user asked: "can you make the Settings more readable? … Can you make tabs for Hotkeys and Defaults / Projects / Pomodoro / Habits".

## 2026-05-06

- [feat]: Edit-task modal duration row: (1) selected chip now uses solid-accent fill + on-accent text so the selection stays unmistakable instead of fading into focus/hover styles, and (2) added a custom-duration text input under the chip row, pre-filled with the current duration so it's visible even when none of the quick-duration chips match. Accepts free-form values (`30m`, `1h`, `1h30m`, `90m`) and tolerates a leading `#d/` tag if pasted. Typing in the field updates the chip selection live and the selected-duration state; clicking a chip mirrors back into the field. Enter still submits. Files: `src/view.ts` (TaskEditModal duration row: `refreshDurButtons`, `durInput`, sync in chip handler + `#$` autocomplete commit; Enter wiring), `src/styles.src.css` (`.dp-duration-btn.is-selected` solid accent fill, `.dp-duration-custom-row` + `.dp-duration-custom-input`). The user asked: "when i pick a duration in the edit task modal, it doesn't highlight. if a duration is already present, it should show in a text box. if i need a custom duration, i can go into the text box, and it shoudl parse my tags (td) for a recommendation".
- [feat]: Date autocomplete: month-name queries now show two rows — the upcoming occurrence and the most recent past occurrence — with the year in the row label so they're distinguishable. So `@jan 5` on 2026-05-06 lists `jan 5 2027` (default, on top) and `jan 5 2026`. Year search spans today ± 4 so a leap-day query like `@feb 29` still finds candidates. Files: `src/dailyNote.ts` (`buildMonthSuggestions` returns both future + past with year-bearing keywords). The user asked: "Jan 5 could mean next jan 5 (future) or past jan 5. have both as options, default to the future being on the top, show the year in the selector".
- [fix]: First click on the plugin pane no longer feels "absorbed". The view was re-rendering on every workspace `active-leaf-change` event, including the one fired synchronously when clicking the inactive pane activated it — so ~100ms after every entering click, `root.empty()` wiped and rebuilt the entire DOM, which read as a flash that swallowed the click. Now the listener only schedules a render when the workspace's active *file* path actually changed (the only thing that listener exists to update — the small "open active file" link). Files: `src/view.ts` (new `lastActiveFilePath`, gate in `onOpen`'s `active-leaf-change` handler, set in `render`). The user asked: "i always have to click the plugin pane once before it activates … i want my mouse to work on the first click".
- [feat]: Date autocomplete (`@`) now recognizes month-name queries with an optional day, e.g. `@apr 5` resolves to the upcoming Apr 5 (rolls forward a year if it'd otherwise be in the past). Both the modal-title trigger and the markdown editor's `EditorSuggest` now allow a single space inside the query when it matches a `month <day>` shape, instead of immediately closing the popover. Files: `src/dailyNote.ts` (`buildMonthSuggestions` + extended `buildDateSuggestions`), `src/view.ts` (new `acceptQuery` opt-in on `TitleSuggestRule`, used by the date rule), `src/main.ts` (`onTrigger` whitespace gate). The user asked: "on the auto-complete for dates, when i do @today it works, i also want @apr 5 and have it work (including the space)".
- [fix]: Edit-task modal: project field now pre-fills with the full `proj/subproj` instead of just the parent. `openTaskEditor` was passing only `task.project` as `initialProject`, so a `#p/workday/ml-design` task opened the modal showing just "workday" — and saving without re-typing the sub-path silently dropped it. Now composes `task.project + "/" + task.subproject` when both are present; `setProjectTag` already accepts the composed form. Files: `src/view.ts` (`openTaskEditor` `initialProjectFull`). The user asked: "you can see that in the Edit Task modal, that the sub-project isn't chosen … i still can't see the chosen sub-project in the edit-task modal".
- [fix]: Timeline-block sub-task rows used to render the raw `#t/HHMM` tag (e.g. `veronica picks up Helena at 6:25 #t/625p`). Now they render the parsed time as a small mono pill at the start of the row (`6:25p · veronica picks up Helena…`), matching how the edit-modal sub-task list already shows it. The body uses `cleanBody` so the tag is stripped from the displayed text. Files: `src/view.ts` (sub-task render branch), `src/styles.src.css` (`.dp-block-subtask-time`). The user asked: "you can see the sub-task has the hashtag for the time. can it render the actual time instead?"
- [fix]: Autocomplete dropdowns (project field + title `##p` / `#@` / `#$` / `@` triggers): make the arrow-key-active row unmistakable and show full `proj/subproj` paths. The prior `surface-hover` background on `.is-active` was nearly invisible on some themes, so users couldn't tell which row was selected even though arrows were wired up. Now uses solid accent bg + white text. Also: project rows now render the full `proj/subproj` as one piece of text instead of project + a faintly-muted `/sub` span (which was disappearing on some themes). Added `scrollIntoView({ block: "nearest" })` on arrow navigation so highlighting an off-screen row scrolls it in. Files: `src/view.ts` (renderers + `updateActive` in both `attachProjectSuggest` and `attachTitleSuggest`), `src/styles.src.css` (`.dp-project-suggest-item.is-active`). The user asked: "I can't see the sub-project on there. just have it be 'proj/subproj' … enable up/down arrows to pick. make sure i can see which is being selected … make sure this works on all dropdowns".
- [feat]: Edit-task modal hotkeys reshuffled to put the action buttons under single keys: `s` show in note, `m` open move, `p` pomodoro, `o` focus description (was `s`). `i` (title), `d` (duration), `x` (delete), `u` (unschedule), `y` (duplicate), `c` (calendar inside the move popover) unchanged. `p` for "focus project" was dropped since `p` is now pomodoro. Also dropped the `title=` attr from every action button — Obsidian renders an `aria-label` tooltip already, so the native browser one was a duplicate ugly tooltip. Files: `src/view.ts` (hotkey table + button attrs). The user asked: "add a hotkey for 'show in note'. let's make this (s). make description 'o'. add one for '(m)ove' and add '(p)omodoro' … get rid of the [system tooltip] that doesn't look good".
- [feat]: Edit-task modal: new "Duplicate" icon button (copy icon, hotkey `y`) inserts a copy of the task line directly under the original. When the task has sub-tasks, a `window.confirm` y/n asks "Copy sub-tasks too?" — OK includes them, Cancel duplicates the parent only. Strips `#tid/` from the copies so duplicate tasks don't share IDs. Files: `src/view.ts` (`duplicateTask`, `onDuplicate` + `hasSubtasks` opts, `runDuplicate`, `y` hotkey). The user asked: "on the bottom of the task modal add a 'duplciate' command with the copy icon. this will be 'y'. it will copy the task. ask if it wants to copy subtasks" / "have a y/n answer on if i want to copy subtasks".
- [feat]: Edit-task modal: `Cmd/Ctrl+Enter` saves from anywhere, including from inside the title/description/project inputs, so the user can commit without first clicking out of the focused field. Plain Enter on title/project still submits (no-modifier-only); the per-input description Cmd+Enter handler was folded into the modal-level keydown to avoid double-fire. The user asked: "add hotkey 'cmd + enter' to save the task while i'm editing it".
- [fix]: Edit-modal hotkeys weren't firing because (1) the modal auto-focused the title input on open, and the handler bails on INPUT focus, and (2) the keydown listener was on `contentEl`, which doesn't see events from the close button (Obsidian's default focus, in `modalEl` outside contentEl). Fix: don't auto-focus title in edit mode, and attach the keydown listener to `modalEl` instead. Also added field-jump hotkeys: `i` focus title (with the same trailing-space trick the auto-focus used to do), `s` focus description, `p` focus project, `d` focus the selected duration button. New mode still auto-focuses title since the user is creating from scratch and needs to type immediately. The user asked: "the hotkeys aren't working … let's actually not activate anything, and create a hotkey (i) for editing the task title. and (s) for going into the task description. (p) for project, (d) for duration".
- [feat]: Edit-task modal: bottom-left "Delete" button (hotkey `x`, with `window.confirm()` permission prompt) drops the task line and its sub-tasks; new "Unschedule" icon button next to Pomodoro (hotkey `u`) strips the `#t/` time tag so the task falls back into the unscheduled bucket. Both close the modal on success. Hotkeys defer to the move stage-two popup when it's open, and skip while the user is typing. Files: `src/view.ts` (callbacks, `deleteTaskLines`, `unscheduleTask`, modal buttons + `onModalKey`), `src/styles.src.css` (`.dp-edit-delete-btn` with `margin-right: auto`). The user asked: "in the bottom left corner of the edit task modal, add a 'delete'. make the hotkey 'x' for it. ask for permission to delete. Add another button for 'u' to move it to 'unscheduled' (strip the time)."
- [fix]: Move-button calendar entry now renders as a calendar-icon button (with `(c)` hotkey) instead of the text "calendar", and opens the same `dp-calendar` widget the navbar uses (extracted into a free `renderPickerCalendar` helper). The widget swaps in over the choices row anchored to the move button; clicking a day moves the task there, Escape returns to the choices row. Files: `src/view.ts` (helper + `moveCalendarPick` opts + popover swap), `src/styles.src.css` (`.dp-edit-move-choice.is-calendar` + `.dp-edit-move-calpopover`). The user asked: "make the calendar a (c) with a calendar icon. use the same calendar widget that's on the navbar".
- [feat]: Move-button cluster gets a 4th "calendar" entry (hotkey `c`). Pops the OS-native date picker pre-set to the currently-viewed day; picking a date moves the task there, cancel/blur leaves it put. Sits after `next week` so the existing 1-4 numeric hotkeys are unchanged. The user asked: "on the 'move file' button have a 4th otpion (c) that's calendar. it brings upa  widget and lets me choose which date to move it to".
- [feat]: Habit-stats pane: workout-goal grid below the reps total in each section. Goals live in the habits-source file as plain hashtag lines like `#x-day/Pushups/25` (daily goal) or `#x-week/Push-ups/50` (weekly goal); the prefix is the configured exercise prefix plus `-day` / `-week` / `-month`. Each goal row shows the exercise name, target chip (`≥25`), and a per-bucket cell that fills when the bucket's completed reps ≥ target. Hover tooltip shows `<reps>/<target> reps (XX%)`. Day/week/month sections each render their matching goals (filtered by period). Files: `src/habits.ts` (added `ExerciseGoal` + `parseExerciseGoals`), `src/habitsView.ts` (goal rows + grid render), `src/styles.src.css` (target chip + grid spacing). The user asked: "let's do exercise goals. the hashtag for this is #x-day/Pushups/25 or #x-week/Push-ups/50 … see if I met my daily goal or not … habits on top and workouts below. reps first, then the same type cards for whether or not i met my exercise goal".
- [feat]: Habit-stats heatmap, week section: simplify column labels. Each week column now shows just the day number ("1", "8", "15") with a month band ("MAR", "APR") spanning above the columns that fall in each month. Lets the cells stay at their current 12px width while keeping the time orientation legible. The user asked: "can you somehow simplify the Week headers? … mar              Apr / 1   8   15  …  5".
- [fix]: Habit-stats heatmap: bump cell size 10→12px and column-gap 8→10px so week ("3/13") and month ("Aug") column labels stop squishing into their neighbors. Track width 18→22px. The user asked: "space the squares out horizontally a little more, as the week headers are still pretty squished. maybe make them 20% wider?"
- [fix]: Habit-stats heatmap: collapse the q1–q4 saturation gradient into a single solid accent fill — habit satisfaction is binary ("did I do it?"), and the gradient was obscuring the signal the user cares about. The user asked: "can you fill in the squares if the habit was satisfied?"
- [fix]: Habit-stats heatmap: completed cells were nearly invisible on light themes (q1 = 50% accent + paper-2 read as near-white), and column labels (`3/13`, `Aug`, etc.) were squished against each other in the 13px tracks. Bumped column-gap from 3px → 8px so labels have breathing room, and bumped saturations to q1 = 75%, q2 = 88%, q3 = 95% so a single completion clearly pops. The user asked: "the cells are blank - also, the titles are squished. Space them out a little more."
- [fix]: Habit-stats heatmap cells were too big and the "no completion" cells were nearly invisible — making it look like only the current day had a box. Cells are now 10px × 10px with 2px corners and a faint inset shadow, matching the project-totals swatches. q0 (no completion) now has the same crisp outline as a colored cell so all days in the window show up consistently. Adjusted hover scale to 1.4× since cells are smaller. The user asked: "why does it only show boxes for one day? it should show them for all the days" and "make the habit stats boxes the same size as the 'project' boxes - they should be small and clean. same as those other project boxes."
- [feat]: Move button in the edit modal now opens a date picker — "tomorrow", the next two day-name buttons, and "next week" (first day of the upcoming week, driven by the habits week-start setting). Each entry moves the whole task to that date; numeric hotkeys 1–4. "Next week" is dropped when its date already appears as one of the next-3-days. The previous whole/incomplete/today cluster is gone. The user asked: "when i click 'move' have it ask me 'when' / and have it make a little pop up 'tomorrow, fri, sat, next week' / it should show the next 3 days and then 'next week' means on the first day of the next week (so sunday)".
- [fix]: Habit stats grid layout was broken on wide panes — cells rendered crammed against the right edge with column labels overlapping each other and only the last cell visible per row. Switched to `display: inline-grid` (so the grid takes its content width instead of stretching), set the grid template inline as literal `140px <14px tracks…>` (no CSS-variable indirection), gave the habit-label column a fixed 140px width with overflow-ellipsis so cells across all rows align, and let column labels overflow visually into adjacent tracks rather than truncating mid-digit. Combined the per-section reps total and per-exercise breakdown onto a single line (`675 reps • Push-ups 250 · Sit-ups 225 · …`). Added a hairline divider between adjacent sections (Day → Week → Month). The user asked: "it's pretty messed up - i can't see the table.s. can you fix it? also, move the reps to one line: 675 • pushups 350 …" and "have a horizontal divider between sections."
- [feat]: Habit stats pane: GitHub-contribution-style layout with one row per habit. Each section (Day / Week / Month) shows a column-header row of bucket labels (date number / week start / month abbrev) and one row per habit of that period; cells are colored by completion count via the `q0..q4` tiers (q1 starts at 50% accent so a single completion is clearly visible). The current bucket gets a subtle accent ring; hover scales the cell. Habit row labels show `slug N/M` where N = cells with at least one completion and M = total cells. Per-section reps total + per-exercise breakdown unchanged. The user asked: "i want a row for each daily/weekly habit. make it look like the Github contribution chart kind of. don't make it exactly like that, because i want a row per habit. so i could have 10 habits stacked if i have 10 daily habits."
- [feat]: Habit click semantics — never delete the task line. Clicking a habit now finds the first `- [ ]` / `- [x]` task line containing the tag and toggles its checkbox in place; if no such line exists, a fresh `- [x]` line is appended. Pre-templated planned-ahead habits are preserved across click/un-click cycles. Completion is now driven by `- [x]` task lines (bare-tag prose lines and unchecked tasks no longer mark a habit complete). When any single daily note has 2+ task lines for the same habit, the chip shows a duplicate badge (`·N` in warn color) and a doubled underline so the user can clean up. The user asked: "When i click on the habit, it rightly creates the todo. But if the todo is already there, it shows up as crossed out. that's not how it should work. Also, if i 'un-check' the habit, it removes it from the file - it shouldn't remove it from the file… If there are duplicates of the habit, have it identify that in the UI somehow."
- [feat]: Habit stats pane: bump heatmap cell contrast (discrete five-tier `q0..q4` color scale instead of a continuous mix), show the date range each row covers next to its title, and add a per-exercise rep breakdown line below each row's totals (sorted by reps, descending). The user asked: "the habit stats are kind of hard to read. can you increase the contrast? also, i want a breakdown of the reps. and the day/week/month should show the dates it's looking at."
- [feat]: Surface a daily "intention" next to the daily-note path in the dashboard header. Configurable bare hashtag (default `#intention`); the rest of the line after the tag becomes the intention, and the first match wins. The user asked: "let's add an intention. again, this is a tag. so if the file has `#intention blah blah` on it, the intention is `blah blah`. And that will sit next to the daily/YYYY-MM-DD.md link. If there are two intentions, just take the first. The `#intention` is a configurable tag".

### Habits tracker: source file, dashboard line, click-toggle, stats pane

- **Requested:** "let's add a habits tracker. The plugin should read
  the settings to find a habits file (default is daily/_habits)…
  habits should have their own hashtag (specified in a setting)… if
  the habits haven't been completed yet this day/week/month, they
  appear below the workout — so it won't disappear with the summary
  collapse… show the uncompleted habits in this format: d:
  call-mom, eat-burrito • w: … • m: … When i click on the habit, it
  will then add a completed task to my file. I can click it a second
  time to un-check it… The other thing is that it should have a
  little button i can click which will show some stats — i want to
  see a heatmap of both exercises and habits… For workouts, find a
  way to sum all the #x/workout/reps." Follow-ups: habits file is
  plain hashtag lines with optional descriptions (no frontmatter);
  writes go to whatever daily note the dashboard is showing; weekly
  habits scan all daily notes in the week; week-start and
  hide-completed are configurable; stats open in a separate pane via
  a `[stats]` link on the habits line.
- **Done:** New `src/habits.ts` owns the parser
  (`parseHabitsFile`), tag regex builder (`buildHabitTagRegex`),
  occurrence counter, line mutators (`appendHabitLine` /
  `removeHabitLine`, with `removeHabitLine` only touching `- [x]`
  lines so prose-tag occurrences are protected), period helpers
  (`weekRange`, `monthRange`, `enumerateDailyNoteDatesInRange`), and
  a memoized `HabitsScanner` that mtime-checks daily-note reads so
  weekly/monthly checks don't thrash the disk on every keystroke.
  New `src/habitsView.ts` registers the `today-habits-stats` view
  with three 10-cell heatmap rows (Day / Week / Month) showing
  completion rate per cell plus total `#x/...` reps in each window.
  `src/view.ts` `renderSection` now renders a `.dp-habits` line
  immediately after `.dp-workout` (which sits in `.dp-header` and
  survives summary collapse), with click-to-toggle chips and a
  trailing `[stats]` link that calls
  `plugin.activateHabitsStatsView()`. `src/settings.ts` adds
  `habitsFile`, `habitPrefix`, `habitWeekStart`,
  `habitsHideCompleted`, and `habitsStatsWindow`, surfaced via
  `renderHabitsSection` between Notes and Templating. `src/main.ts`
  instantiates the scanner, registers the stats view, and adds an
  `open-habits-stats` command. CSS for `.dp-habits*` and
  `.dp-heatmap*` lives next to `.dp-workout` in
  `src/styles.src.css`, with cell colors driven by a
  `--dp-heatmap-intensity` custom property.

### Edit modal: lift `#tid/<id>` out of the title into a clickable header pill

- **Requested:** "when a task has a tid in it, and i open the modal, i
  can see the tid in the title. I have to delete it from the title, or
  it will duplicate the task id. i shouldn't have to do that. parse out
  the task id and put the id in the top right corner, which is a
  clickable tag so i can see in the search the other instances of that
  task id."
- **Done:** `cleanBody` (`src/view.ts`) now strips `#tid/<id>` so the
  title input no longer carries it (which was the source of the
  on-save duplication). The tag is surfaced as a small pill in the top
  right of the modal header (`.dp-edit-tid-pill`, `src/styles.src.css`)
  that, when clicked, opens Obsidian's global search for
  `tag:#tid/<id>` so the user can find every other occurrence of that
  task id. The tid is still preserved in the underlying line on save
  because `setTaskTitle` keeps the existing tag block.

### Apply daily-note template when files are created via link-following

- **Requested:** "when i click the hyperlink to the date that doesn't
  exist, it's not rendering the template. it renders the template when i
  click the arrows or the calendar on the plugin, but when i click on a
  link inside my obsidian vault it doesn't create the template. let's
  make sure that if a date file is created afresh inside of daily/ folder
  that it loads the template."
- **Done:** Added `applyDailyNoteTemplateIfEmpty` in `src/dailyNote.ts`
  and registered a `vault.on("create")` listener in `TodayPlugin.onload`
  (`src/main.ts`). When any new `.md` file lands in the configured daily
  folder with a basename that parses against the daily-note format and
  is empty, the listener writes the expanded template into it. The empty
  guard means our existing `ensureDailyNote` path (which writes content
  up front) is left alone, and any other plugin that fills the file
  first wins.

### Daily-note template: bare `<@today>` is wall-clock; add `<@…-rel>` parsed from filename

- **Requested:** Pick semantics (A) — bare `<@today>` resolves to real
  wall-clock today; `<@today-rel>` and friends resolve relative to the
  filename's date. Parse the filename so `-rel` works in any file with a
  date-shaped name, not just inside `ensureDailyNote`.
- **Done:** Reworked `expandDateTemplate` in `src/dailyNote.ts` to take
  `fileBasename` instead of a pre-resolved `refDate`. The regex now
  recognizes an optional `-rel` suffix; bare keywords anchor to
  `new Date()` while `-rel` keywords parse the basename via the daily-note
  format string in moment strict mode. `ensureDailyNote` derives the
  basename from the resolved path and passes it through. If the filename
  doesn't match the expected format the `-rel` placeholder is left
  intact so the mismatch is visible in the file.

### Add CLAUDE.md + initial CHANGELOG.md

- **Requested:** "updae CLAUDE.md to create a changelog - after you
  finish, update CHANGELOG.md with 1) what was requested and 2) what you
  did."
- **Done:** Created `CLAUDE.md` documenting the changelog convention
  (newest-first by date, "Requested" + "Done" per entry) and seeded
  `CHANGELOG.md` by backfilling the recent settings reshuffle, the
  autocomplete intro tweak, the `@`-trigger date autocomplete, and the
  in-progress template substitution.

### Edit modal: bigger action icons, labeled move popover

- **Requested:** "these icons are too small. make them bigger. Also, when
  i click the 'move' icon, i want words to pop up, the words showing the
  hotkeys like move (w)hole or move (i)ncompleted. I also don't want the
  original move icon to disappear (right now it disappears)."
- **Done:** Bumped `.dp-edit-icon-btn` from 32→40 px (svg 16→20) and the
  Save button padding to match (`src/styles.src.css`). Replaced the move
  popover's icon sub-buttons with text labels — `Move (t)oday`, `Move
  (w)hole`, `Move (i)ncomplete` — with the hotkey letter highlighted in
  the accent color via a new `.dp-edit-move-hotkey` span. The original
  move icon now stays visible while the popover is open; clicking it
  again closes the popover (`src/view.ts`).

### Settings layout: move #ta into Pomodoro, split Task ID into its own section

- **Requested:** "in the settings, move the #ta into the top of the Pomo
  section. Create a new section for Task ID"
- **Done:** Moved the "Actual time tag prefix" (`#ta`) row from the end of
  the Defaults section to the top of the Pomodoro section. Lifted "Task ID
  tag prefix" and "Task ID length" out of Defaults into a new "Task ID"
  section that renders between Pomodoro and Autocomplete
  (`src/settings.ts`).

### Note that autocomplete triggers are mainly a mobile convenience

- **Requested:** "on the autocomplete description, add 'These are mostly
  conveniences for mobile where typing is slow'."
- **Done:** Appended that sentence to the Autocomplete intro paragraph
  (`src/settings.ts`).

### `@`-trigger date autocomplete inserting daily-note links

- **Requested:** A new `@` autocomplete that handles relative dates —
  `@today`, `@yesterday`, `@tomorrow`, `@Nd` — inserting a link to the
  matching daily note. Optional moment.js display format (default
  `ddd, MMM D, YYYY` → "Mon, Mar 5, 2026"). Wiki vs. markdown form per
  vault settings. Must work in tasks (`- [ ] @today …`) and in plain files,
  including at the start of a line.
- **Done:** Added `dateTrigger` to `AutocompleteSettings` (default `@`) and
  `dateLinkFormat` to `TodaySettings` (default `ddd, MMM D, YYYY`). New
  `buildDateSuggestions` and `buildDateLinkInsert` helpers in
  `src/dailyNote.ts` honor `app.vault.getConfig('useMarkdownLinks')` to
  pick wiki vs. markdown form. Wired into both the in-editor
  `EditorSuggest` (`src/main.ts`) and the modal title-input rule list
  (`src/view.ts`). Trigger detection now prefers the longer trigger when
  two overlap (so `#@` keeps winning over its own `@` suffix), and the
  column-0 "must have leading non-whitespace" rule now only applies to
  `#`-starting triggers — `@today` at the start of a line fires the
  picker. Settings UI exposes the trigger field and the date-link format.

### Daily-note template: substitute `<@today>` etc. on note creation

- **Requested:** "the templating parser should look for `<@yesterday>`,
  and if so, it will fill in the template value." Followed by:
  introduce `-rel` variants (`<@today-rel>`, `<@yesterday-rel>`) that
  resolve against the new file's filename so relative relationships
  survive when notes are pre-created for future dates.
- **Done (partial):** Added `resolveDateKeyword` and `expandDateTemplate`
  in `src/dailyNote.ts`. `ensureDailyNote` now passes the template through
  the expander, replacing `<@today>`, `<@yesterday>`, `<@tomorrow>`, and
  `<@Nd>` with daily-note links resolved against the note's own date.
  `DailyNoteFallback` gained a `dateLinkFormat?: string` field; the five
  `ensureDailyNote` call sites in `src/view.ts` now pass it.
- **Open:** Awaiting clarification on `-rel` semantics — specifically
  whether the bare `<@today>` form should switch to wall-clock today
  (with `-rel` becoming the file-relative variant), or whether `-rel` is
  an explicit alias for the file-relative behavior already implemented.
  Not yet committed.
